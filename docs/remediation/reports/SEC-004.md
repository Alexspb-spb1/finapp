# SEC-004 — Перенести создание компании на сервер

## Итоговый статус
READY_FOR_INDEPENDENT_REVIEW

## Branch / commit
- branch: `remediation/SEC-004-create-company-function`
- base SHA: `6d5092cbd01a991b8e63a567d3cdea128b6c9908` (= `origin/main` at preflight time, exact match to `EXPECTED_BASE_SHA`)
- head SHA: `db9bdd728e947d20274bc48d364b0dd52878a345`
- PR: https://github.com/Alexspb-spb1/finapp/pull/12 (Draft)
- CI run: https://github.com/Alexspb-spb1/finapp/actions/runs/31329935113 — jobs `ci` and `functions` both green; `Deploy to GitHub Pages` run for this head shows `skipped` (https://github.com/Alexspb-spb1/finapp/actions/runs/31329986323) — no Firebase/Pages deploy happened

## Проверенное исходное состояние

Перепроверен фактический (не документированный) код `src/store/authStore.ts` и `firestore.rules` перед началом реализации:

- `authStore.register()` создавал `companyId = 'co_' + crypto.randomUUID()` **на клиенте**, затем писал `users/{uid}`, `companies/{companyId}`, `company_data/{companyId}` тремя параллельными `setDoc()` **с клиента**, с `role: 'admin'` в теле `users/{uid}`.
- Ошибка Firestore-записи (`Promise.all([...]).catch`) только логировалась (`console.error`) — функция всё равно возвращала `{ ok: true }` и переводила `authDataStatus` в `'ready'`.
- `onAuthStateChanged` при отсутствии `users/{uid}` (любая причина: сбой записи, гонка, чужой Auth-аккаунт без профиля) **автоматически** создавал `recoveredUser`/`recoveredCompany` с `role: 'admin'` и переводил приложение в `'ready'` — вне зависимости от того, удалась ли повторная запись в Firestore (fallback работал даже при отказе записи, используя только in-memory состояние).
- Не было отдельного идемпотентного механизма/retry для повторного запуска setup после сбоя — единственный путь начать заново был создать нового Firebase Auth пользователя.
- Конкурентное повторение `register()` (двойной клик/двойная вкладка) не имело server-side защиты — каждый вызов писал свою пару документов независимо.

### Как зафиксирован baseline (red→green)

Написан `src/store/authStore.createCompany.test.ts` — это одновременно и red-proof, и итоговый (green, коммитящийся) mandatory "Client unit" test suite из п.7 задания: тесты кодируют ТРЕБУЕМОЕ (безопасное) поведение и должны провалиться против исходной реализации, если она его не обеспечивает.

Запуск против НЕИЗМЕНЁННОГО `authStore.ts` (до реализации SEC-004):

```
Test Files  1 failed (1)
     Tests  10 failed | 1 passed (11)
```

Полный вывод сохранён в `functions`-независимом scratch-файле сессии и приведён ниже (репрезентативный фрагмент, остальное — аналогичные провалы по каждому из 8 пунктов задания):

```text
 ❯ src/store/authStore.createCompany.test.ts (11 tests | 10 failed) 163ms
     × a successful callable response leads to ok:true and a ready state loaded from Firestore 11ms
     × registration never writes companies/company_data/users documents directly via setDoc 8ms
     × a callable failure after Auth user creation returns setup_incomplete, not a false ok:true 3ms
     × a callable failure never puts the app into the ready state 2ms
     × a callable failure creates no local admin/company fallback 2ms
     × an invalid callable response (thrown by the typed API module) is treated as an error, not success 2ms
     × retry (completeCompanySetup) after a callable failure does NOT create a second Auth user 2ms
     × retry reuses the SAME idempotency key as the original attempt 4ms
     × completeCompanySetup with no pending state and no signed-in user returns setup_incomplete without calling the callable 2ms
     × a signed-in user with no users/{uid} document lands in setup_incomplete, not ready — and no doc is auto-created 4ms

stdout | ... > a signed-in user with no users/{uid} document lands in setup_incomplete...
[authStore] Auto-recovered missing Firestore docs for uid: uid_orphan

FAIL ... > a callable failure after Auth user creation returns setup_incomplete, not a false ok:true
AssertionError: expected { ok: true } to deeply equal { ok: false, error: 'setup_incomplete' }

FAIL ... > a callable failure creates no local admin/company fallback
AssertionError: expected { id: 'uid_new_example_test', role: 'admin', ... } to be null

FAIL ... > retry reuses the SAME idempotency key as the original attempt
TypeError: authStore.completeCompanySetup is not a function
```

Каждый red-провал соответствует конкретному пункту задания:
- `ok:true` при отказе callable/Firestore → «Firestore error during registration can return a false success».
- `currentUser`/`role:'admin'` появляются несмотря на отказ → «missing profile can trigger local privilege-escalating recovery» / «client itself assigns the first admin role».
- `completeCompanySetup is not a function` → «after Auth user creation there is no safe retry for company setup».
- `[authStore] Auto-recovered missing Firestore docs...` в stdout — прямое доказательство fail-open recovery-ветки.
- «registration never writes ... via setDoc» проваливался, т.к. старый код писал ровно `companies/`, `company_data/`, `users/` тремя `setDoc()`.

Единственный прошедший тест из 11 (`email_taken` early-return) не затрагивается SEC-004 и не должен был измениться.

Red-состояние не коммитилось отдельно — после захвата вывода сразу выполнена реализация (см. ниже), и тот же файл тестов прогнан повторно до зелёного (см. «Проверки»).

## Что изменено

### Backend (`functions/`)
- **`functions/src/schemas/company.ts`** (новый) — `CreateCompanyRequestSchema` (`.strict()`, `idempotencyKey`/`ownerName`/`companyName` non-empty-after-trim, `legalType: 'ooo'|'ip'`, `inn` опционален и валидируется под конкретный `legalType` — 10 цифр для `ooo`, 12 для `ip`) и `CreateCompanyResponseSchema` (`{ companyId }` — ничего больше).
- **`functions/src/lib/bootstrapIdempotency.ts`** (новый) — узкий bootstrap-идемпотентный механизм для «первой компании» пользователя, отдельный от `runIdempotent()` (который привязан к уже существующему `companyId` и не трогается/не ослабляется). Receipt хранится по фиксированному пути `user_bootstrap/{uid}`, ключ — **только uid**, не сырой idempotency key — это и есть механизм, которым «один uid не может создать две первые компании даже с разными ключами» (см. «Архитектура» ниже). Переиспользует `computeFingerprint`/`validateIdempotencyKey` из `idempotency.ts` (последняя сделана `export`, поведение не изменено).
- **`functions/src/index.ts`** — добавлен callable `createCompany` (SEC-003 `authzProbe` не тронут). `requireAuth()` — единственный источник `uid`; email — только из `adminAuth.getUser(auth.uid)` (Admin Auth), никогда из payload. **Не** вызывает `requireVerifiedEmail` (осознанное bootstrap-исключение, обязательное применение — SEC-013); клиент по-прежнему отправляет письмо верификации сразу после создания Auth-пользователя. Все документы (`companies/{companyId}`, `companies/{companyId}/members/{uid}`, `company_data/{companyId}`, `users/{uid}` bridge, bootstrap-receipt, audit event) создаются в ОДНОЙ Firestore-транзакции; чтений внутри `run()` нет — правило "все чтения перед всеми записями" тривиально выполнено (единственное чтение — receipt — уже произошло до `run()`).
- **`functions/test/unit/company-schema.test.ts`** (новый, 20 тестов) — валидация схемы: unauthenticated покрыт в `authz.test.ts`; пустое/whitespace-only имя владельца/компании; неверный `legalType`; неверная длина/формат ИНН для `ooo`/`ip`; лишние `uid`/`ownerUid`/`role`/`companyId`/`createdAt`/`currency`; отсутствующий `idempotencyKey`; отсутствие утечки значения в ошибке; форма ответа (`{companyId}` — ничего больше).
- **`functions/test/unit/bootstrapIdempotency.test.ts`** (новый, 2 теста) — пустой/слишком длинный ключ отклоняется ДО обращения к Firestore (проверено через `Proxy`, который бросает при любом обращении).
- **`functions/test/emulator/createCompany.test.ts`** (новый, 14 тестов, реальные Auth+Firestore+Functions Emulators) — успешное создание; неверифицированный email всё ещё может пройти bootstrap (осознанное исключение); подмена `uid`/`ownerUid`/`companyId`/`role` в payload отклоняется как `invalid_request`; retry с тем же ключом возвращает тот же `companyId` без второй компании; тот же ключ + другой payload → `idempotency_conflict`; **разные** ключи для одного uid → тоже `idempotency_conflict` (ровно одна компания); два параллельных идентичных запроса → ровно одна компания у всех успешных ответов; два параллельных запроса с разными ключами → ровно один успех, ровно одна компания; ровно одно active admin membership после retry; невалидные данные/невалидный ИНН не оставляют частичных документов (`companies`/`users` пусты); ошибки не содержат секретных значений из запроса.
- **`functions/test/emulator/helpers.ts`** — добавлены `callCreateCompany`, `getCompanyDoc`, `getMembershipDoc`, `getCompanyDataDoc`, `getUserDoc`, `countCompaniesOwnedBy` (только новые функции, существующие не менялись).

### Client (`src/`)
- **`src/lib/companyApi.ts`** (новый) — единственная точка вызова callable `createCompany`; строгая Zod-валидация ответа (`{companyId}`, `.strict()`) — невалидный ответ трактуется как ошибка, никогда как частичный успех.
- **`src/lib/firebase.ts`** — добавлен экспорт `functions` (Firebase Functions client) и подключение Functions Emulator внутри существующего `if (resolved.useEmulators && ...)` блока (порт `5001` — фиксированный, из `firebase.json`; отдельного поля host для functions-эмулятора в `ResolvedFirebaseEnv` нет, переиспользован `firestoreEmulatorHost`, т.к. оба эмулятора всегда на одной машине в dev/CI). `firebaseEnvCore.mjs`/`.d.mts` не тронуты (вне allowed scope).
- **`src/store/authStore.ts`**:
  - `register()` теперь только (1) создаёт Firebase Auth пользователя, (2) отправляет письмо верификации (как раньше), (3) сохраняет resumable-состояние (`idempotencyKey` + имя/компания/тип/ИНН, **без пароля**) в `localStorage` под `finapp_pending_setup`, (4) делегирует в новый `completeCompanySetup()`.
  - Новый `completeCompanySetup()` — вызывает `callCreateCompany()`, при успехе читает КАНОНИЧЕСКИЕ документы из Firestore (`loadReadyStateAfterSetup()` — не фабрикует локальный объект), при любой ошибке (сеть/сервер/невалидный ответ) возвращает `{ ok: false, error: 'setup_incomplete' }`, не создаёт локальный fallback, не помечает `ready`. Тот же метод используется для retry — не создаёт нового Auth-пользователя, переиспользует сохранённый `idempotencyKey`.
  - `onAuthStateChanged`: ветка «`users/{uid}` отсутствует» **больше не создаёт** `recoveredUser`/`recoveredCompany` с `role: 'admin'` — очищает privileged-состояние и переводит `authDataStatus` в `'setup_incomplete'` (fail-closed вместо fail-open). Остальная логика `onAuthStateChanged` (recovery `companies/{companyId}` для СУЩЕСТВУЮЩЕГО профиля с «висячим» companyId, multi-company, data_error-обработка) **не тронута** — вне явного scope SEC-004 (см. «Известные ограничения»).
  - `AuthDataStatus` дополнен значением `'setup_incomplete'`; `AuthError` дополнен значением `'setup_incomplete'`.
  - `authStore.createCompany()` (добавление ДОПОЛНИТЕЛЬНОЙ компании существующим админом — другой flow, не регистрация) **не тронут** — вне scope, см. «Известные ограничения».
- **`src/pages/Register.tsx`** — новый шаг `'setup_incomplete'`: при `result.error === 'setup_incomplete'` НЕ выполняется `navigate('/')`, показывается отдельный экран с кнопкой «Повторить создание компании», вызывающей `authStore.completeCompanySetup()` (без повторного ввода пароля).
- **`src/store/authStore.createCompany.test.ts`** (новый, 11 тестов) — см. «red→green» выше; это и есть итоговый mandatory "Client unit" suite.

## Почему изменения входят в текущий пункт
Все изменения — прямая реализация пп. 3–7 формальной спецификации SEC-004 (серверный `createCompany`, bootstrap-идемпотентность, клиентский flow регистрации с safe retry). Ничего за пределами создания компании при регистрации не менялось.

## Затронутые файлы
```text
REMEDIATION_PLAN.md                          (SEC-004: [ ] → [-])
functions/src/index.ts                       (+createCompany)
functions/src/lib/idempotency.ts             (export validateIdempotencyKey)
functions/src/lib/bootstrapIdempotency.ts    (новый)
functions/src/schemas/company.ts             (новый)
functions/test/unit/company-schema.test.ts   (новый)
functions/test/unit/bootstrapIdempotency.test.ts (новый)
functions/test/emulator/createCompany.test.ts    (новый)
functions/test/emulator/helpers.ts           (+6 helper-функций)
src/lib/companyApi.ts                        (новый)
src/lib/firebase.ts                          (+functions client/emulator)
src/store/authStore.ts                       (register/completeCompanySetup/onAuthStateChanged)
src/pages/Register.tsx                       (+setup_incomplete UI)
src/store/authStore.createCompany.test.ts    (новый — red→green suite)
docs/remediation/reports/SEC-004.md          (этот отчёт)
```

## Архитектура `createCompany`

**Request** (`CreateCompanyRequestSchema`, `.strict()`):
```ts
{ idempotencyKey: string; ownerName: string; companyName: string; legalType: 'ooo'|'ip'; inn?: string }
```
`uid`/`ownerUid`/`role`/`companyId`/`createdAt`/`currency` и любые другие поля — отклоняются схемой целиком (лишние ключи → `invalid_request`, значение никогда не попадает в текст ошибки — переиспользован существующий `validateRequest()`/`AppError`, который не имеет параметра details).

**Response**: `{ companyId: string }` — ничего больше.

**Источник каждого привилегированного значения**:
| Поле | Источник |
|---|---|
| `uid` (owner/admin) | `requireAuth(request).uid` — платформенно-верифицированный ID-токен |
| `email` | `adminAuth.getUser(auth.uid).email` (Admin Auth), никогда payload |
| `companyId` | `db.collection('companies').doc().id` — случайный Firestore ID, генерируется сервером |
| `role` (membership) | жёстко `'admin'` в коде — не из payload |
| `createdAt`/`updatedAt` (membership) | `FieldValue.serverTimestamp()` |
| `name`/`legalType`/`inn` (company) | из провалидированного payload (не привилегированные — контролируются самим создателем) |

**Bootstrap-идемпотентность** (`functions/src/lib/bootstrapIdempotency.ts`) — receipt по фиксированному пути `user_bootstrap/{uid}` (ключ — **только uid**, не сырой idempotency key):
- Тот же key + тот же fingerprint payload → возвращается закэшированный `{companyId}`, мутация не повторяется.
- Тот же key + другой payload → `idempotency_conflict`.
- **Другой** key для того же uid → тоже `idempotency_conflict` — потому что receipt лежит по uid, а не по хешу ключа: второй вызов читает ТОТ ЖЕ документ и видит, что `stored.idempotencyKey !== idempotencyKey`. Это и есть требуемое «один uid не может создать две первые компании даже с разными ключами».
- Receipt и бизнес-мутация пишутся ОДНИМ `run()` внутри ОДНОЙ `db.runTransaction()` — тот же однофазный паттерн, что и у `runIdempotent()`, без промежуточного «in_progress» состояния.
- Конкурентность: обе параллельные транзакции читают `user_bootstrap/{uid}` в свой read set; Firestore optimistic concurrency заставляет проигравшую транзакцию повторно выполниться и увидеть уже закоммиченный receipt — тот же механизм, на котором держится `assertNotLastAdmin()` из SEC-003. Подтверждено эмулятор-тестами (`Promise.allSettled` на двух параллельных вызовах — ровно один company).

**Список создаваемых документов** (все — одной транзакцией):
```text
companies/{companyId}
companies/{companyId}/members/{uid}
company_data/{companyId}
users/{uid}                       — временный совместимый bridge, см. ниже
user_bootstrap/{uid}              — bootstrap-идемпотентный receipt
companies/{companyId}/audit_events/{autoId}  — через существующий writeAuditEvent()
```

## Ограничение `users/{uid}` compatibility bridge
Сервер по-прежнему пишет `users/{uid}` в минимальной legacy-совместимой форме (`{id, name, email, role: 'admin', companyId, createdAt}`), т.к. клиентский `parseLegacyUserDocument`/`LegacyUserSchema` (SEC-002) и большая часть `authStore.ts` (`onAuthStateChanged`, `getEffectiveRole()`, `switchCompany()`, страница «Пользователи») всё ещё читают роль/companyId именно оттуда. Это временный **серверный** мост — клиент сам это поле больше не пишет и не может (Firestore Rules `users/{userId}` create по-прежнему запрещают `role`/`companyId`/`companies` в клиентской записи; Admin SDK эти Rules не проходит, так что сервер может писать поле, которое клиенту запрещено). Каноническим источником авторизации остаётся `companies/{companyId}/members/{uid}` (ADR-001). Полное удаление legacy-полей из `users/{uid}` и client-side чтения — SEC-005/SEC-008.

## Критерии приемки
- [x] `createCompany` callable создаёт `companies/{companyId}`, `companies/{companyId}/members/{uid}`, `company_data/{companyId}` атомарно/идемпотентно
- [x] Client больше не создаёт `companies`/`company_data`/роль `admin` напрямую при регистрации
- [x] После создания Firebase Auth user вызывается `createCompany`; при неудаче — `setup_incomplete`, безопасный повторный вызов, без local admin fallback
- [x] Идентификатор компании создаётся сервером/Firestore (`db.collection('companies').doc().id`), не `Date.now()`
- [x] Регистрация не оставляет пользователя в ложно успешном состоянии (red-tests это доказали против старой реализации)
- [x] Компания и admin membership создаются сервером
- [x] Повтор (тот же idempotency key) не создаёт вторую компанию — доказано unit- и real-emulator-тестами, включая параллельные вызовы
- [ ] Полное устранение legacy `users/{uid}` полей и client-side auto-recovery для «висячего» companyId — вне scope, см. «Известные ограничения» (SEC-005/SEC-008)

## Проверки
| Команда | Результат | Примечание |
|---|---|---|
| `npm ci` (root) | PASS | уже установлено, зависимости не менялись |
| `npm run lint` (root) | PASS | 0 errors, 1 pre-existing warning в `Balance.tsx` (не относится к SEC-004) |
| `npm run typecheck` (root) | PASS | |
| `npm run test:staging-preflight` | PASS | 5/5 сценариев |
| `npx tsc --noEmit -p tests/rules/tsconfig.json` | PASS | Rules не менялись |
| `npm run test:unit` (root) | PASS | 125/125; было 4 test-файла до SEC-004 (bankStatementImport, schemas/auth, schemas/company, authStore.validation), стало 5 (+authStore.createCompany.test.ts) — количество тестов не уменьшилось |
| `npm run test:rules` (root, Firestore Emulator) | PASS | 77/77 — `firestore.rules` не менялись, тест подтверждает отсутствие регрессии |
| `npm run build` (root) | PASS | `tsc -b && vite build`, один pre-existing chunk-size warning |
| `npm audit` (root) | Известный baseline | 20 уязвимостей (postcss/protobufjs/react-router/uuid/vite/xlsx) — идентичны SEC-003 baseline, `package.json` не менялся, новых зависимостей не добавлено |
| `npm ci --prefix functions` | PASS | portable Node 22.20.0 |
| `npm run lint --prefix functions` | PASS | 0 problems |
| `npm run typecheck --prefix functions` | PASS | |
| `npm run test:unit --prefix functions` | PASS | 68/68; было 4 test-файла до SEC-004 (authz, authz-firestore-errors, errors, idempotency), стало 6 (+company-schema.test.ts, +bootstrapIdempotency.test.ts) — количество тестов не уменьшилось |
| `npm run build --prefix functions` | PASS | `tsc` |
| `npm run test:emulator --prefix functions` | PASS | 47/47 (реальные Auth+Firestore+Functions Emulators); было 4 test-файла до SEC-004 (audit, authzProbe, idempotency, lastAdmin), стало 5 (+createCompany.test.ts, 14 тестов) — количество тестов не уменьшилось |
| `npm audit --prefix functions` | Известный baseline | 10 moderate — идентичны SEC-003 baseline (транзитивные `firebase-admin`/`firebase-tools`), `package.json` не менялся |
| `npm audit --omit=dev --prefix functions` | Известный baseline | 7 moderate (только prod-зависимости) |
| `git diff --check` | PASS | только предупреждение о LF/CRLF (Windows), не ошибка |
| `git status --short` | см. выше | все файлы — в allowed scope |

`npm exec -- tsc --noEmit -p tests/rules/tsconfig.json` выполнена (см. строку 5 выше — команда запущена как `npx tsc ...` с тем же эффектом).

`test:run`/`test:e2e` — **NOT AVAILABLE**: таких npm-скриптов нет ни в `package.json`, ни в `functions/package.json` (не добавлялись — вне scope).

## Фактический вывод существенных тестов
```text
$ npm run test:unit
 Test Files  5 passed (5)
      Tests  125 passed (125)

$ npm run test:rules
 Test Files  1 passed (1)
      Tests  77 passed (77)

$ npm run build
✓ built in 4.03s

$ npm run test:unit --prefix functions
 Test Files  6 passed (6)
      Tests  68 passed (68)

$ npm run test:emulator --prefix functions
 Test Files  5 passed (5)
      Tests  47 passed (47)
```

Red-run (до реализации, против исходного `authStore.ts`) — см. раздел «Проверенное исходное состояние» выше.

## Security review
- **uid никогда не берётся из payload** — `requireAuth(request).uid`; спуфинг `uid`/`ownerUid`/`companyId`/`role` отклоняется схемой (`invalid_request`), проверено unit- и emulator-тестами.
- **email никогда не берётся из payload** — только `adminAuth.getUser(auth.uid)`.
- **companyId никогда не клиентский** — случайный Firestore doc id, генерируется сервером.
- **Ошибки клиенту** — только `{ appCode }` через существующий `toSafeHttpsError()`/`AppError` (SEC-003), без утечки email/ИНН/названия компании — проверено выделенным тестом («errors returned to the client never contain secret-like values...») в обоих (unit + emulator) наборах.
- **Атомарность** — вся мутация (company/membership/company_data/users-bridge/receipt/audit) — одна Firestore-транзакция; невалидные данные или конфликт идемпотентности не оставляют частичных документов (проверено emulator-тестами прямым чтением через Admin SDK).
- **requireVerifiedEmail сознательно не вызывается** здесь — задокументированное bootstrap-исключение (иначе только что созданный пользователь не смог бы зарегистрировать компанию вообще, т.к. email ещё физически не мог быть подтверждён). Обязательное принудительное применение — SEC-013. Письмо верификации всё равно отправляется.
- **Firestore Rules не менялись** — `users/{userId}` create по-прежнему запрещает клиенту писать `role`/`companyId`/`companies` (Admin SDK, которым пишет `createCompany`, эти Rules не проходит — ожидаемо и безопасно, т.к. только серверный код имеет Admin-доступ).

## Данные и миграция
- Миграции нет. Новая коллекция `user_bootstrap/{uid}` создаётся по мере регистрации новых пользователей; существующие пользователи (уже имеющие компанию) этой коллекцией не затрагиваются.
- Существующие production/staging данные не читались и не менялись (вся работа — только Emulator Suite и unit-тесты, per CLAUDE.md §5; `EXTERNAL_ACTION_APPROVED`/`PRODUCTION_ACTION_APPROVED` не запрашивались и не требовались).

## Ручная проверка
Не выполнялась (нет запроса пользователя на ручной browser-flow вне Emulator Suite); поведение полностью покрыто автоматизированными emulator- и unit-тестами, включая реальный HTTP-путь через Functions Emulator (`firebase emulators:exec ... functions`).

## Rollback
Локальная task-ветка, ничего не задеплоено (ни `firebase deploy`, ни production/staging). Откат — `git checkout origin/main -- .` для затронутых файлов, либо просто не мержить/удалить ветку `remediation/SEC-004-create-company-function`. Данных для отката нет (новая коллекция `user_bootstrap/{uid}` создаётся только для новых регистраций через новую функцию — при откате кода эти документы становятся неиспользуемыми, но не ломающими ничего, т.к. никакой другой код их не читает).

## Известные ограничения
- **`users/{uid}` legacy compatibility bridge** остаётся (см. раздел выше) — полное устранение legacy-полей и client-side чтения роли из `users/{uid}` — **SEC-005**.
- **`onAuthStateChanged`'s "companies/{companyId} missing" recovery-ветка** (для СУЩЕСТВУЮЩЕГО профиля с висячим `companyId`, не для регистрации) не тронута — по-прежнему создаёт `companies/{companyId}`/`company_data/{companyId}` с клиента при отсутствии документа компании. Это отдельный fail-open путь, не входящий в явный список SEC-004 (который про создание компании ПРИ РЕГИСТРАЦИИ) — рекомендуется **SEC-005/SEC-008**.
- **`authStore.createCompany()`** (добавление ДОПОЛНИТЕЛЬНОЙ компании существующим авторизованным пользователем — multi-company flow, отдельная кнопка в UI, не регистрация) по-прежнему пишет `companies`/`company_data` напрямую с клиента; Firestore Rules это по-прежнему разрешают (`allow create: if isSignedIn() && ...`). Не входит в текст SEC-004 (который явно про `src/store/authStore.ts`/`src/pages/Register.tsx` в контексте **регистрации**), но представляет тот же класс риска — рекомендуется отдельная задача **SEC-005** (перенести и этот путь на сервер, переиспользуя `createCompany`/новый non-bootstrap путь).
- **`requireVerifiedEmail` не применяется** к `createCompany` — осознанно, обязательное применение отложено до **SEC-013**.
- **UI-уровневые пункты mandatory-тестов** («UI does not navigate to home on setup_incomplete», «retry button only triggers completion of server-side setup») проверены **ревью кода**, а не автоматизированным component-тестом: в репозитории нет React Testing Library/jsdom-инфраструктуры для `.tsx`-компонентов (только `vitest` в node-окружении для стора/схем), а добавление такой инфраструктуры — вне минимального scope SEC-004 (задача явно запрещает "обновлять все зависимости"/добавлять непрофильные devDependencies). Сама логика (`if (result.ok) navigate(...)`, отдельная `else if (result.error === 'setup_incomplete')` ветка, `handleRetrySetup()` вызывает исключительно `authStore.completeCompanySetup()`) покрыта тем, что она **тонкая обёртка** над уже протестированным на уровне `authStore` поведением — `Register.tsx` не содержит собственной бизнес-логики для этого пути.
- `npm audit` (root и functions) показывает baseline-уязвимости, идентичные зафиксированным в SEC-003 — SEC-004 не добавил новых зависимостей и не увеличил их число.

## Дополнительные находки вне scope
- См. «Известные ограничения» выше (`users/{uid}` bridge, `onAuthStateChanged` company-recovery, `authStore.createCompany()` multi-company path, `requireVerifiedEmail`) — все зафиксированы как рекомендации для SEC-005/SEC-008/SEC-013, не исправлялись в этом PR.

## Diff summary
```text
 REMEDIATION_PLAN.md                |   2 +-
 functions/src/index.ts             | 141 +++++++++++++++++++++--
 functions/src/lib/idempotency.ts   |   2 +-
 functions/test/emulator/helpers.ts |  37 ++++++
 src/lib/firebase.ts                |   9 ++
 src/pages/Register.tsx             |  49 +++++++-
 src/store/authStore.ts             | 229 +++++++++++++++++++++++--------------
 7 files changed, 371 insertions(+), 98 deletions(-)
```
(плюс новые файлы вне diff --stat baseline: `functions/src/lib/bootstrapIdempotency.ts`, `functions/src/schemas/company.ts`, `functions/test/unit/company-schema.test.ts`, `functions/test/unit/bootstrapIdempotency.test.ts`, `functions/test/emulator/createCompany.test.ts`, `src/lib/companyApi.ts`, `src/store/authStore.createCompany.test.ts`, `docs/remediation/reports/SEC-004.md`)

## Следующий разрешённый пункт
- `SEC-005` — см. `REMEDIATION_PLAN.md`. НЕ начат и не анализировался.
