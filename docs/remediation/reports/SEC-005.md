# SEC-005 — Создать и выполнить backfill membership-документов

## Итоговый статус
PARTIAL — implementation and emulator verification complete; staging is now
authorized (`EXTERNAL_ACTION_APPROVED: SEC-005` / `ENVIRONMENT: staging`)
and one real `--mode dry-run` was executed against `finapp-staging` (see
«Staging authorization» ниже) — `apply` against staging was deliberately
NOT executed this round. Production backfill remains not authorized and
not executed.

> **Обновление после независимого ревью (`REVIEW_RESULT: CHANGES REQUIRED`).**
> Все 7 категорий блокирующих замечаний исправлены — см. раздел
> «Исправления по итогам независимого аудита» ниже. Раздел добавлен, а не
> заменяет предыдущий отчёт: весь текст ниже до этого раздела описывает
> состояние ветки ДО независимого ревью и сохранён как есть.
>
> **Второе обновление после повторного независимого ревью
> (`REVIEW_RESULT: CHANGES REQUIRED`, второй раунд).** Ещё 7 категорий
> блокирующих замечаний исправлены — см. раздел «Исправления по итогам
> ПОВТОРНОГО независимого аудита» в самом конце отчёта. Ни один из
> предыдущих разделов не удалён и не переписан.
>
> **Третье обновление после третьего независимого ревью
> (`REVIEW_RESULT: CHANGES REQUIRED`, третий раунд).** Ещё 4 категории
> блокирующих замечаний исправлены — см. раздел «Исправления по итогам
> ТРЕТЬЕГО независимого аудита» в самом конце отчёта. Ни один из
> предыдущих разделов не удалён и не переписан.
>
> **Коррекция после четвёртого раунда ревью того же PR
> (`REVIEW_RESULT: CHANGES REQUIRED`, оставшийся fail-open из третьего
> раунда).** Фикс #3 третьего раунда («existing orphaned membership
> integrity»), реализованный выше, содержал fail-open: pair-level `exclude`
> decision мог снять dangling membership из `unresolvedOrphans`, хотя
> документ физически оставался в Firestore — если у компании был ДРУГОЙ
> валидный admin, `applyAllowed`/`verify` могли стать `true` с повреждённым
> документом всё ещё на месте. Исправлено отдельным commit `fix(sec-005):
> keep dangling memberships fail-closed` — см. раздел «Коррекция: dangling
> memberships остаются fail-closed» в самом конце отчёта. Раздел третьего
> раунда НЕ переписан (описывает состояние ДО этой коррекции), только
> дополнен корректирующим разделом ниже.
>
> **Staging authorization.** Владелец репозитория передал
> `EXTERNAL_ACTION_APPROVED: SEC-005` / `ENVIRONMENT: staging` (production
> явно запрещён: «Production запрещён»). Мигратор разрешён для staging
> (production остаётся безусловно заблокирован), добавлены
> staging-allow/production-deny тесты, выполнен ровно один реальный
> `--mode dry-run` против `finapp-staging` — см. раздел «Staging
> authorization (EXTERNAL_ACTION_APPROVED: SEC-005)» в самом конце отчёта.
> `apply` НЕ выполнялся. Ни один предыдущий раздел не удалён и не переписан.

## Branch / commit
- branch: `remediation/SEC-005-membership-backfill`
- base SHA: `a3256672c848d53ad3a19f6ecb45e2dbedeb5b4b` (= `origin/main` at preflight, exact match to `EXPECTED_BASE_SHA`)
- result SHA: см. commit ниже (одна коммит-задача); head SHA не дублируется в этом файле, чтобы не создавать самоссылку — указан в PR body и финальном сообщении сессии.

## Проверенное исходное состояние

Перепроверены (полностью прочитаны перед реализацией): `CLAUDE.md`,
`REMEDIATION_PLAN.md` (раздел SEC-005), `docs/adr/001-company-membership-and-roles.md`,
`docs/remediation/reports/SEC-004.md`, `src/schemas/auth.ts`,
`functions/src/schemas/auth.ts`, `functions/src/lib/authz.ts`, `package.json`,
`.github/workflows/ci.yml`, `.gitignore`.

Фактическое состояние до этой задачи:
- Канонический путь `companies/{companyId}/members/{uid}` (ADR-001) **нигде
  не создавался** — ни клиентом (SEC-004 переносит только создание НОВОЙ
  компании при регистрации, создавая membership для владельца, но НЕ
  затрагивает существующих legacy-пользователей), ни каким-либо
  инструментом.
- Все существующие пользователи по-прежнему имеют авторизационные данные
  только в legacy-полях `users/{uid}.role`/`companyId`/`companies[]`.
- Никакого backfill-инструмента, dry-run/apply/verify/rollback-цикла или
  checksum-контракта не существовало в репозитории — `scripts/backfill-memberships.ts`,
  весь `scripts/lib/*` для этой задачи и `docs/migrations/MEMBERSHIP_BACKFILL.md`
  отсутствовали до этой ветки.

### Red→green evidence

Baseline — объективное отсутствие возможности: до этой ветки не существовало
ни одного файла, способного безопасно (dry-run, checksums, идемпотентность,
create-only, project guards) перенести legacy-данные в `members/*`; ручная
правка через Firebase Console была единственной альтернативой, без каких-либо
гарантий идемпотентности/аудита. Тесты и реализация писались итеративно
(не строгий pre-implementation TDD), но реальные red-провалы зафиксированы
и исправлены **до** финализации отчёта — не скрыты:

1. Баг разделителя ключей (`relationKey`) — первая версия `types.ts` содержала
   повреждённый управляющий символ (не обычный пробел) внутри template
   literal, использованного как разделитель пары companyId/uid. Из-за этого
   `scripts/lib/legacyMapping.test.ts` («never turns an unknown role into
   admin») и три теста `scripts/lib/planner.test.ts` («skips an existing
   membership…», «never overwrites…», «a corrupted existing membership…»)
   **реально упали** с `AssertionError`, показав склеенную строку вида
   `companyId: "co_a<CTRL>u1"` и `uid: undefined` вместо корректно
   разобранной пары. Исправлено: `relationKey`/`splitRelationKey` теперь
   строятся через `String.fromCharCode(58, 58)` (символ `::`) —
   детерминированно, без риска скрытого символа — и весь код-потребитель
   (`legacyMapping.ts`, `firestoreReaders.ts`, `backfill-memberships.ts`,
   тесты) переведён на них вместо ручного построения ключей через шаблонные
   строки.
2. Баг last-admin-гейта — исходная реализация `buildPlan()` включала
   `companyId` каждого **orphan** (ссылка на несуществующую компанию) в
   множество компаний, проверяемых на «есть ли admin», из-за чего тест
   «an unresolved orphan blocks apply until acknowledged via an exclude
   decision» **реально проваливался** (`applyAllowed` оставался `false`
   даже после подтверждающего decision). Исправлено: гейт теперь построен
   только по компаниям с хотя бы одной **подтверждённой** (после decisions)
   relation — несуществующая компания никогда не участвует в проверке
   «есть ли у неё admin», что и логически корректно (проверять наличие
   admin у компании, которой нет, бессмысленно).

Оба бага были обнаружены реальными прогонами `npx vitest run scripts` в ходе
разработки этой ветки и исправлены до финализации коммита — сами тесты,
зафиксировавшие их, сохранены без изменений и проходят зелёным в финальном
состоянии (см. «Тесты и результаты»).

## Точный scope

Только backfill membership-документов (создание файлов):

```text
scripts/backfill-memberships.ts        — CLI entry point
scripts/lib/firebaseAdmin.ts           — project/environment guards + Admin SDK init
scripts/lib/types.ts                   — shared domain types
scripts/lib/legacyMapping.ts           — pure legacy → candidate-relation extraction
scripts/lib/decisions.ts               — decisions-file validation
scripts/lib/membershipValidation.ts    — existing-membership classification
scripts/lib/planner.ts                 — combines extraction+decisions+existing into a plan
scripts/lib/firestoreReaders.ts        — thin Admin SDK read wrappers
scripts/lib/checksum.ts                — canonical JSON + SHA-256 checksums
scripts/lib/report.ts                  — versioned JSON report schema + safe stdout summary
scripts/lib/cli.ts                     — argv parsing
docs/migrations/MEMBERSHIP_BACKFILL.md — full walkthrough/reference
docs/remediation/reports/SEC-005.md    — этот отчёт
```

Плюс обязательные тесты (см. «Тесты и результаты»), `tsconfig.scripts.json`
(+ ссылка на него в `tsconfig.json`), `package.json`/`package-lock.json`
(root devDependency `firebase-admin` + 2 npm-скрипта), `.gitignore`
(безопасные локальные паттерны для report/decisions), однострочная правка
`REMEDIATION_PLAN.md` (`SEC-005: [ ] → [-]`), и **один новый шаг**
`- run: npm run test:migration` в `.github/workflows/ci.yml` (`ci`-job,
сразу после `test:rules`) — единственное разрешённое изменение этого файла,
per заданию («CI разрешено менять только для обязательного запуска новых
migration/emulator-тестов»).

**Не менялись и не тронуты** (проверено `git diff origin/main -- src functions
firestore.rules firestore.indexes.json firebase.json` — пусто): `src/**`,
`functions/**`, `firestore.rules`, `firestore.indexes.json`, `firebase.json`,
`.github/workflows/deploy*`.

**Явно не исправлялось** (as instructed): legacy-чтение ролей в `authStore`,
`getEffectiveRole()`, client-side company recovery, создание доп. компании,
Users UI, удаление legacy-полей из `users/{uid}`, приглашения/управление
участниками — всё это соседние задачи, не SEC-005.

## Архитектура мигратора

```text
CLI (cli.ts, argv parsing)
  → assertEnvironmentGuard() (firebaseAdmin.ts) — БЕЗ credential acquisition/I-O
  → initFirestore() (Admin SDK, ADC resolution delegated entirely to firebase-admin)
  → readAllUsers/readAllCompanies/readAllExistingMemberships (firestoreReaders.ts)
  → extractLegacyRelations() (legacyMapping.ts, ЧИСТАЯ функция) → confirmed/conflicts/orphans/ownerAnomalies
  → validateDecisions() (decisions.ts, ЧИСТАЯ функция) → typed Decision[]
  → buildPlan() (planner.ts, ЧИСТАЯ функция) → plannedCreates/skipped/unresolved*/companiesWithoutAdmin/applyAllowed
  → [apply only] create-only writes (DocumentReference.create()) + read-back → observedChecksum
  → [verify only] read current state → observedChecksum vs targetChecksum
  → [rollback-from-report only] strict per-document precondition check → delete or refuse
  → writeReport() (report.ts) — ТОЛЬКО в абсолютный путь вне репозитория
  → printSafeSummary() — ТОЛЬКО safe aggregates в stdout
```

Все «трудные» правила (дедупликация, конфликты, orphan/owner-anomaly
классификация, last-admin-гейт, decisions-резолюция) реализованы в чистых,
не завязанных на Firestore функциях (`legacyMapping.ts`, `decisions.ts`,
`planner.ts`, `membershipValidation.ts`, `checksum.ts`) — это то, что делает
78 из 93 тестов возможными БЕЗ эмулятора.

## Матрица конфликтов/orphan/owner-anomaly

| Тип | Условие | Куда попадает | Может ли decision разрешить |
|---|---|---|---|
| `role_mismatch` | 2+ различных ВАЛИДНЫХ роли для одной пары | conflicts | `confirm_role`/`exclude` |
| `invalid_role` | Все claims для пары — неизвестная/пустая/битая роль | conflicts | `confirm_role`/`exclude` |
| `user_id_mismatch` | `users/{uid}.id` присутствует и ≠ ID документа | conflicts (для ВСЕХ relations этого документа) | `confirm_role`/`exclude` |
| `owner_role_not_admin` | `ownerId` имеет подтверждённую НЕ-admin роль | conflicts | `confirm_role`/`exclude` |
| `existing_membership_conflict` | Существующий `members/{uid}` не совпадает с кандидатом | conflicts | только `accept_existing` |
| `missing_company` | legacy-поле ссылается на несуществующую компанию | orphans | только `exclude` (никогда `confirm_role` — decision не может создать компанию) |
| `missing_user` | `ownerId` ссылается на несуществующего пользователя | orphans | только `exclude` |
| `owner_without_admin_membership` | `ownerId` без какого-либо membership claim | ownerAnomalies | `confirm_role`/`exclude` |

## Counts / checksum contract

См. `docs/migrations/MEMBERSHIP_BACKFILL.md`, разделы «Counts and checksum
contract» и «Idempotency» — полная спецификация. Кратко: SHA-256 над
каноническим (рекурсивно отсортированным по ключам) JSON, порядок relations
всегда `companyId` → `uid`, timestamps исключены из checksum намеренно
(server-assigned, не часть «того же логического состояния»). `runId` —
`crypto.randomUUID()`, никогда `Date.now()`.

## Project guards

`assertEnvironmentGuard()` (`scripts/lib/firebaseAdmin.ts`) — pure function,
9 unit-тестов, плюс 1 real-emulator тест подтверждает отказ ДО любого
Firestore I/O при несовпадении project ID. `--environment`/`--project` не
имеют default-значений. Staging/production execution в этом цикле
дополнительно заблокированы безусловно на уровне `main()`
(`scripts/backfill-memberships.ts`) — возвращает код 4 независимо от флагов.

## Тесты и результаты

| Suite | Файлы | Тестов | Результат |
|---|---|---|---|
| `scripts/lib/*.test.ts` (pure, no I/O) | 7 | 78 | PASS |
| `scripts/backfill-memberships.emulator.test.ts` (real Firestore Emulator) | 1 | 15 | PASS |
| **Итого `npm run test:migration`** | **8** | **93** | **PASS** |

Emulator suite покрывает (реальные, не mock-only, доказательства): dry-run
по умолчанию пишет 0 документов; apply мигрирует primary membership;
компания без admin блокирует ВЕСЬ apply (0 записей); неизвестная роль
никогда не становится admin; missing company не создаёт membership; owner
без подтверждённого membership не получает auto-admin; существующий
точно совпадающий membership пропускается без перезаписи; существующий
ОТЛИЧАЮЩИЙСЯ membership никогда не перезаписывается; валидный
`confirm_role` decision создаёт подтверждённую membership; невалидный
decisions-файл отклоняется до какой-либо записи; project guard блокирует
до I/O; **повторный apply создаёт 0 новых, 0 изменённых, тот же
`targetChecksum`, те же timestamps** (идемпотентность); `verify` обнаруживает
удалённый после apply документ; `rollback-from-report` удаляет только
документы, созданные ИМЕННО этим run; rollback отказывается удалять
документ, изменённый после backfill.

Каждый тест в `scripts/backfill-memberships.emulator.test.ts` запускает
РЕАЛЬНЫЙ CLI как дочерний процесс (`node scripts/backfill-memberships.ts …`),
а не импортирует внутренние функции — доказательство «как оператор реально
запустит», а не white-box вызов.

Изоляция между emulator-тестами — `beforeEach` очищает ВСЕ данные проекта
через Firestore Emulator clear-data endpoint (`DELETE
/emulator/v1/projects/demo-finapp/databases/(default)/documents`) — не
оставляет fixture residue между тестами и после всего прогона (Firestore
Emulator процесс завершается вместе с `firebase emulators:exec`).

## Проверки

| Команда | Результат | Примечание |
|---|---|---|
| `npm ci` (root) | PASS | добавлена `firebase-admin` devDependency |
| `npm run lint` (root) | PASS | 0 errors, 1 pre-existing warning в `Balance.tsx` (не относится к SEC-005) |
| `npm run typecheck` (root) | PASS | `tsconfig.scripts.json` подключён через `tsconfig.json` references — `tsc -b` типизирует `scripts/**` автоматически |
| `npm run test:staging-preflight` | PASS | 5/5 сценариев, не менялся |
| `npx tsc --noEmit -p tests/rules/tsconfig.json` | PASS | Rules не менялись |
| `npm run test:unit` (root) | PASS | 147/147 — без изменений (scripts/ не входит в `vitest run src`) |
| `npm run test:rules` (root, Firestore Emulator) | PASS | 77/77 — без изменений |
| `npm run test:migration` (НОВАЯ, Firestore Emulator, теперь и в CI) | PASS | **93/93** (78 pure + 15 real-emulator) |
| `npm run build` (root) | PASS | `tsc -b && vite build`; `scripts/**` вне `vite build` (только `src/`), бандл не изменился |
| `npm audit` (root) | Известный baseline + документированная дельта | **24** уязвимости (было 20 в SEC-004 report baseline) — **+4 moderate**, все транзитивные через новую `firebase-admin` (`uuid`/`gaxios`/`teeny-request`/`retry-request` цепочка) — тот же класс риска, что уже присутствует в `functions/package.json`'s собственном `firebase-admin`, теперь и в root. Зависимости не обновлялись сверх необходимого для SEC-005. |
| `npm ci --prefix functions` | PASS | portable Node 22.20.0 |
| `npm run lint --prefix functions` | PASS | `functions/**` не менялся |
| `npm run typecheck --prefix functions` | PASS | |
| `npm run test:unit --prefix functions` | PASS | 68/68 — без изменений |
| `npm run build --prefix functions` | PASS | |
| `npm run test:emulator --prefix functions` | PASS | 50/50 — без изменений |
| `npm audit --prefix functions` | Известный baseline | 10 moderate — без изменений, `functions/package.json` не менялся |
| `git diff --check` | PASS | только предупреждения о LF/CRLF (Windows), не ошибки |
| `git diff --name-only origin/main...HEAD` | см. «Diff summary» | |
| `git diff origin/main -- src functions firestore.rules firestore.indexes.json firebase.json` | PASS (пусто) | подтверждено — ни один запрещённый путь не тронут |

`npm run test:run` — **NOT AVAILABLE**: такого npm-скрипта нет в `package.json` (не добавлялся — вне scope).

## Фактический вывод существенных тестов

```text
$ npm run test:migration
 Test Files  8 passed (8)
      Tests  93 passed (93)

$ npm run test:unit
 Test Files  7 passed (7)
      Tests  147 passed (147)

$ npm run test:rules
 Test Files  1 passed (1)
      Tests  77 passed (77)

$ npm run build
✓ built in 4.78s

$ npm run test:unit --prefix functions
 Test Files  6 passed (6)
      Tests  68 passed (68)

$ npm run test:emulator --prefix functions
 Test Files  5 passed (5)
      Tests  50 passed (50)
```

## Security review

- **Fail-closed везде**: неизвестная/пустая/битая роль никогда не получает
  default; отсутствующий документ никогда не подразумевает `admin`;
  `ownerId` — только сигнал для проверки, никогда прямой источник admin.
- **Create-only writes**: `DocumentReference.create()` — Admin SDK сам
  обеспечивает precondition «документ отсутствует»; race между planning и
  apply не может привести к тихой перезаписи.
- **Никаких credentials в коде/логах**: `initFirestore()` никогда не читает
  service-account JSON вручную — делегирует ADC resolution целиком Admin
  SDK; stdout содержит только safe aggregates (project ID, mode, checksums,
  counts) — проверено `printSafeSummary()`, отдельно от полного отчёта.
- **PII вне Git**: полный отчёт и decisions-файл принудительно проверяются
  на абсолютный путь ВНЕ репозитория (`assertSafeReportPath()`) —
  зафиксировано тестом.
- **Project guard до I/O**: несовпадение CLI/env project ID отклоняется до
  `initFirestore()` — подтверждено real-emulator тестом (report не создаётся
  вообще).

## Данные и миграция

- **Staging/production данные НЕ читались и НЕ изменялись** — вся работа
  этого цикла выполнена исключительно против Firestore Emulator
  (`demo-finapp`), как явно разрешено (`LOCAL_EMULATOR_ACTIONS_APPROVED: SEC-005`).
  `EXTERNAL_ACTION_APPROVED`/`PRODUCTION_ACTION_APPROVED` не были переданы
  и не запрашивались.
- dry-run: подтверждено (реальным emulator-тестом) — 0 записей.
- idempotency: подтверждено — повторный apply создаёт 0/изменяет 0,
  сохраняет timestamps, даёт тот же `targetChecksum`.
- checksums: SHA-256, canonical JSON, order-independent — реализация и
  10 unit-тестов в `scripts/lib/checksum.test.ts`.
- rollback: подтверждено реальным emulator-тестом (удаляет только
  созданное, отказывается удалять изменённое после backfill).

## Ручная проверка

Не выполнялась вне автоматизированных emulator-тестов — весь цикл
dry-run→decisions→apply→verify→rollback покрыт `scripts/backfill-memberships.emulator.test.ts`,
запускающим реальный CLI как дочерний процесс против настоящего Firestore
Emulator (не белый ящик — тот же бинарный путь, которым воспользуется
оператор).

## Rollback

`--mode rollback-from-report --from-report <apply-report.json>` — см.
`docs/migrations/MEMBERSHIP_BACKFILL.md`, раздел «Rollback», для полного
контракта. Для ОТКАТА ЭТОГО PR (не backfill-данных, а самого кода): PR не
слит, ничего не задеплоено — достаточно не мержить/удалить ветку
`remediation/SEC-005-membership-backfill`. Никаких production/staging
данных этот PR не затрагивал, откатывать во внешних средах нечего.

## Известные ограничения

- **Partial-write-failure**: код для честного отражения частичного отказа
  записи реализован (`writeFailures[]` в отчёте, каждый create — независимая
  попытка), но НЕ проверен реальным сценарием отказа записи в этом цикле —
  создать детерминированный частичный сбой в Firestore Emulator без
  инвазивного вмешательства в тестовое окружение не входило в объём этого
  раунда. Помечено как остаточный test gap.
- **Firestore-query-order-independence checksum** проверен unit-тестами
  (`computeRelationSetChecksum` sorted-input tests) напрямую на уровне
  checksum-функции, но НЕ через намеренно переупорядоченный реальный
  Firestore-запрос в эмуляторе (Firestore возвращает документы в
  недетерминированном для нас порядке естественно, но тест не форсирует
  два РАЗНЫХ порядка одного и того же набора явно).
- **Staging rehearsal и production backfill НЕ выполнялись и НЕ разрешены**
  в этом цикле — CLI по коду отказывается запускаться против
  `--environment staging|production` безусловно (`main()`'s explicit guard),
  независимо от флагов. Требуется отдельное `EXTERNAL_ACTION_APPROVED:
  SEC-005` / `ENVIRONMENT: staging`, затем отдельное
  `PRODUCTION_ACTION_APPROVED` с `BACKUP_REFERENCE`/`ROLLBACK_REFERENCE`.
- **`npm audit` (root)**: +4 moderate от новой `firebase-admin`
  devDependency (тот же транзитивный класс уязвимостей, что уже
  существовал в `functions/package.json`) — задокументировано выше, не
  скрыто, не исправлялось вне scope SEC-005.
- Легаси-источники строго ограничены четырьмя полями, указанными в задании
  (`users/{uid}.companyId`/`.role`/`.companies[]`, `companies/{companyId}.ownerId`)
  — никакие другие поля никогда не читаются как источник авторизации.

## Дополнительные находки вне scope

- `authStore.getEffectiveRole()` и client-side company recovery (описаны в
  `docs/remediation/reports/SEC-004.md`, «Известные ограничения») по-прежнему
  не тронуты — этот backfill НЕ переключает чтение прав на `members/*`
  (это отдельная задача, `SEC-003`/`SEC-011` по ADR-001) и НЕ удаляет
  legacy-поля из `users/{uid}` (тоже отдельная задача).
- `authStore.createCompany()` (multi-company путь) по-прежнему пишет
  `companies`/`company_data` напрямую с клиента — не входит в scope SEC-005
  (backfill касается ТОЛЬКО существующих legacy-связей, не нового кода).

## Diff summary
```text
 .github/workflows/ci.yml                      |   2 +
 .gitignore                                    |  11 +
 REMEDIATION_PLAN.md                           |   2 +-
 docs/migrations/MEMBERSHIP_BACKFILL.md        | 336 ++++++++++++++
 package-lock.json                             | 646 ++++++++++++++++++++++++--
 package.json                                  |   5 +-
 scripts/backfill-memberships.emulator.test.ts | 292 ++++++++++++
 scripts/backfill-memberships.ts               | 320 +++++++++++++
 scripts/lib/checksum.test.ts                  |  76 +++
 scripts/lib/checksum.ts                       |  77 +++
 scripts/lib/cli.test.ts                       |  59 +++
 scripts/lib/cli.ts                            |  92 ++++
 scripts/lib/decisions.test.ts                 |  74 +++
 scripts/lib/decisions.ts                      | 109 +++++
 scripts/lib/firebaseAdmin.test.ts             |  82 ++++
 scripts/lib/firebaseAdmin.ts                  |  90 ++++
 scripts/lib/firestoreReaders.ts               |  50 ++
 scripts/lib/legacyMapping.test.ts             | 146 ++++++
 scripts/lib/legacyMapping.ts                  | 166 +++++++
 scripts/lib/membershipValidation.test.ts      |  50 ++
 scripts/lib/membershipValidation.ts           |  42 ++
 scripts/lib/planner.test.ts                   | 141 ++++++
 scripts/lib/planner.ts                        | 120 +++++
 scripts/lib/report.ts                         | 122 +++++
 scripts/lib/types.ts                          | 150 ++++++
 tsconfig.json                                 |   3 +-
 tsconfig.scripts.json                         |  27 ++
 27 files changed, 3247 insertions(+), 43 deletions(-)
```
(плюс `docs/remediation/reports/SEC-005.md`, этот файл, не включён в статистику выше по причине самоссылочности размера)

## Исправления по итогам независимого аудита

Независимый ревьюер вернул `REVIEW_RESULT: CHANGES REQUIRED` с 7 категориями
блокирующих замечаний по исходной реализации (см. раздел выше). Все 7
исправлены в этом же PR, отдельным commit `fix(sec-005): address
independent backfill review` поверх исходного HEAD
`b8e2def0736c74e4fdfe1947dd485a42dc00e4e4`.

### 1. Last-admin-гейт проверял не КАЖДУЮ существующую компанию

**Было**: гейт «есть ли хотя бы один admin» строился только по компаниям,
для которых нашлась хотя бы одна confirmed legacy-relation — компания без
единой legacy-связи (и без admin) молча проходила apply.
**Стало**: `buildPlan()` принимает новый обязательный параметр
`allCompanyIds: ReadonlySet<string>` (вычисляется в CLI как
`new Set(companies.map(c => c.docId))` из `readAllCompanies()`) и итерирует
именно его. Плюс: admin-статус компании считается ТОЛЬКО через
`isStrictlyValidActiveMembership(uid, data) && data.role === 'admin'` —
повреждённый документ с `role: 'admin'` больше не защищает apply.
Файлы: `scripts/lib/planner.ts`, `scripts/lib/firestoreReaders.ts`
(`computeExistingActiveAdmins`), `scripts/backfill-memberships.ts`.

### 2. `accept_existing` мог принять НЕ строго валидный membership

**Было**: классификация существующего membership была бинарной
(`exact_match` / `differs_or_invalid`) — `accept_existing` фактически мог
подтвердить документ с неизвестной ролью, `status !== 'active'`, чужим
`uid`, отсутствующими timestamps или лишними полями.
**Стало**: новая 4-значная классификация
`'not_found' | 'exact_match' | 'differs_but_valid' | 'invalid'`
(`scripts/lib/membershipValidation.ts`, новая экспортируемая
`isStrictlyValidActiveMembership()`). `accept_existing` резолвит ТОЛЬКО
`'differs_but_valid'`; `'invalid'` безусловно становится нерешённым
`existing_membership_conflict`, независимо от decision.

### 3. Rollback был уязвим к подмене source-report и TOCTOU-гонке

**Было**: rollback не проверял схему/mode/environment/projectId source
report и не защищал `get()`→`delete()` от изменения документа между чтением
и удалением.
**Стало**: новый чистый модуль `scripts/lib/rollbackValidation.ts` —
`validateSourceReportForRollback(raw, {environment, projectId})` — отвергает
(без единого удаления): не-объект, неверный `schemaVersion`, `mode !==
'apply'`, несовпадение environment/projectId, нарушение канонического пути
в `rollbackManifest`, дубликаты пар, записи без соответствия в
`createdPaths`/`plannedCreates` (включая ожидаемые role/status/timestamps).
Само удаление (`rollbackOneEntry` в `backfill-memberships.ts`) теперь
использует Firestore-precondition `ref.delete({ lastUpdateTime:
snap.updateTime })` — изменение документа между чтением и удалением
гарантированно приводит к отказу удаления, а не к race.

### 4. Observed checksum мог подставлять ожидаемые значения вместо прочитанных

**Было**: при отсутствии документа в read-back checksum мог быть посчитан
по ожидаемому (target) значению, маскируя частичный сбой записи.
**Стало**: новый чистый модуль `scripts/lib/observedState.ts` —
`computeObservedState(target, readBack)` — для каждой target-relation берёт
ТОЛЬКО фактически прочитанное; при отсутствии — `missing[]` +
sentinel-значение `'MISSING'` в checksum-входе (никогда не подставляет
target-значение); при расхождении — `differing[]`. Используется идентично
для apply (read-back после записи) и verify. Добавлена проверка exit-кода:
`mode === 'apply' && observedChecksum !== targetChecksum` → код 1.

### 5. Проверка безопасного пути отчёта выполнялась ПОСЛЕ начала Firestore I/O и не применялась к decisions/source-report

**Было**: `--report-path` проверялся отдельной inline-функцией уже после
`assertEnvironmentGuard`/`initFirestore`; `--decisions-file` и `--from-report`
вообще не проверялись на «вне репозитория».
**Стало**: новый общий модуль `scripts/lib/pathSafety.ts` —
`assertPathOutsideRepo(label, path, repoRoot)` — резолвит symlink-и через
`fs.realpathSync` (включая ещё не созданные конечные файлы), сравнивает
регистронезависимо на `win32`/`darwin`. Применяется КО ВСЕМ трём флагам
(`--report-path`, `--decisions-file`, `--from-report`) сразу после
`parseCliArgs()`, до `assertEnvironmentGuard`/`initFirestore`/любого I/O —
невалидный путь гарантированно даёт 0 записей.

### 6. Повреждённые legacy-источники молча пропускались

**Было**: пользователь без единой пригодной relation просто исчезал из
отчёта; malformed-записи в `companies[]` не отражались нигде; пара с ОДНОЙ
валидной И ОДНОЙ невалидной claim на одну и ту же роль тихо подтверждалась
через валидную claim.
**Стало**: `scripts/lib/legacyMapping.ts` теперь возвращает
`unknownUsers: {uid, reason: 'no_usable_relations'}[]` (пользователь без
единой пригодной relation) и `malformedClaims: {uid, reason:
'malformed_companies_entry'}[]` (не-объектные/без `companyId` записи в
`companies[]`, ранее — тихий `continue`). Новый `ConflictReason` —
`'mixed_role_validity'`: пара с валидной И невалидной role-claim теперь
безусловно становится конфликтом, а не auto-confirmed relation. `planner.ts`
дополнительно фильтрует `unknownUsers` — пользователь не считается unknown,
если для него уже есть строго валидный существующий membership где-либо.

### 7. `readAllExistingMemberships()` мог захватить чужие/вложенные `members`-документы

**Было**: чтение полагалось на `collectionGroup('members')` без проверки
формы полного пути — документ в любой вложенной `members`-подколлекции
(не строго `companies/{companyId}/members/{uid}`) мог повлиять на
planning/checksums/admin-гейт.
**Стало**: `firestoreReaders.ts` валидирует каждый путь: ровно 4 сегмента,
`segments[0] === 'companies'`, `segments[2] === 'members'`,
`uid === doc.id` — всё остальное отбрасывается до попадания в Map.

### Новые red→green тесты

| Сценарий | Уровень | Файл |
|---|---|---|
| Компания без единой legacy-relation и без admin блокирует apply | emulator | `backfill-memberships.emulator.test.ts` |
| Повреждённый (extra-field) existing admin не проходит гейт | emulator + unit | `backfill-memberships.emulator.test.ts`, `membershipValidation.test.ts` |
| `accept_existing` отклоняет повреждённый existing membership | emulator | `backfill-memberships.emulator.test.ts` |
| `accept_existing` отклоняет disabled existing membership | emulator | `backfill-memberships.emulator.test.ts` |
| `accept_existing` принимает строго валидный differs_but_valid | emulator + unit | `backfill-memberships.emulator.test.ts`, `planner.test.ts` |
| Tampered (wrong-project) rollback report отклонён БЕЗ удаления | emulator + unit | `backfill-memberships.emulator.test.ts`, `rollbackValidation.test.ts` |
| Rollback report с `mode !== apply` отклонён БЕЗ удаления | emulator + unit | `backfill-memberships.emulator.test.ts`, `rollbackValidation.test.ts` |
| Изменение документа между чтением и rollback-удалением → отказ | emulator | `backfill-memberships.emulator.test.ts` (существующий тест повторно подтверждён под новой `lastUpdateTime`-реализацией) |
| Partial apply failure не даёт совпадающий observed checksum | unit | `observedState.test.ts` (см. «Известные ограничения» — почему не на уровне emulator) |
| Невалидный `--report-path` (внутри репозитория) блокируется до записи | emulator + unit | `backfill-memberships.emulator.test.ts`, `pathSafety.test.ts` |
| `--decisions-file` внутри репозитория отклоняется | emulator | `backfill-memberships.emulator.test.ts` |
| Пара с валидной И невалидной role-claim → конфликт, не auto-confirm | emulator + unit | `backfill-memberships.emulator.test.ts`, `legacyMapping.test.ts` |
| Пользователь без единой пригодной relation → unknown, не исчезает | emulator + unit | `backfill-memberships.emulator.test.ts`, `legacyMapping.test.ts` |
| Foreign `members`-документ вне канонического пути игнорируется | emulator | `backfill-memberships.emulator.test.ts` |

### Обновлённые счётчики тестов

| Suite | Файлы | Тестов | Результат |
|---|---|---|---|
| `scripts/lib/*.test.ts` (pure, no I/O) | 10 (+3 новых: `pathSafety`, `rollbackValidation`, `observedState`) | 127 (было 78) | PASS |
| `scripts/backfill-memberships.emulator.test.ts` (real Firestore Emulator) | 1 | 27 (было 15, +12 новых) | PASS |
| **Итого `npm run test:migration`** | **11** | **154** (было 93) | **PASS** |

### Затронутые файлы этого раунда

Новые: `scripts/lib/pathSafety.ts`, `scripts/lib/pathSafety.test.ts`,
`scripts/lib/rollbackValidation.ts`, `scripts/lib/rollbackValidation.test.ts`,
`scripts/lib/observedState.ts`, `scripts/lib/observedState.test.ts`.
Изменённые: `scripts/backfill-memberships.ts`, `scripts/lib/types.ts`,
`scripts/lib/membershipValidation.ts`, `scripts/lib/membershipValidation.test.ts`,
`scripts/lib/planner.ts`, `scripts/lib/planner.test.ts`,
`scripts/lib/legacyMapping.ts`, `scripts/lib/legacyMapping.test.ts`,
`scripts/lib/firestoreReaders.ts`, `scripts/lib/report.ts`,
`scripts/backfill-memberships.emulator.test.ts`,
`docs/migrations/MEMBERSHIP_BACKFILL.md`, этот файл.
Без изменений вне scope: `src/**`, `functions/**`, `firestore.rules`,
`firestore.indexes.json`, `firebase.json` (подтверждено `git diff
origin/main...HEAD -- src functions firestore.rules firestore.indexes.json
firebase.json` — пусто).

### Повторный прогон всех проверок после исправлений

| Команда | Результат |
|---|---|
| `npm run typecheck` (root) | PASS |
| `npm run lint` (root) | PASS — 0 errors, тот же 1 pre-existing warning в `Balance.tsx` |
| `npx vitest run scripts/lib` | PASS — 127/127 |
| `npm run test:migration` (Firestore Emulator, JDK 21) | PASS — 154/154 (11 файлов) |
| `npm run test:unit` (root) | PASS — 147/147, без изменений |
| `npm run test:staging-preflight` | PASS — 5/5, без изменений |
| `npx tsc --noEmit -p tests/rules/tsconfig.json` | PASS |
| `npm run test:rules` (Firestore Emulator) | PASS — 77/77, без изменений |
| `npm run build` (root) | PASS |
| `npm ci` (root) | PASS |
| `npm audit` (root) | 22 уязвимости (было 24 в предыдущем раунде отчёта — улучшение, зависимости не менялись, дельта от upstream advisory-данных) |
| `npm ci --prefix functions` (portable Node 22.20.0) | PASS |
| `npm run lint --prefix functions` | PASS |
| `npm run typecheck --prefix functions` | PASS |
| `npm run build --prefix functions` | PASS |
| `npm run test:unit --prefix functions` | PASS — 68/68, без изменений |
| `npm run test:emulator --prefix functions` | PASS — 50/50, без изменений |
| `npm audit --prefix functions` | 10 moderate — без изменений (`functions/**` не тронут) |
| `git status --short` (до commit) | только файлы этого раунда, перечислены выше |
| `git diff --check` | только LF→CRLF предупреждения (Windows), не ошибки |
| `git diff origin/main...HEAD -- src functions firestore.rules firestore.indexes.json firebase.json` | пусто — подтверждено |

### Известные ограничения этого раунда

- **Partial-write-failure на уровне live-emulator race** по-прежнему не
  воспроизведён как реальный сценарий отказа записи в самом Firestore
  Emulator — CLI выполняется как единый синхронный дочерний процесс
  (`spawnSync`), и детерминированно оборвать ОДНУ конкретную запись из
  N в реальном emulator run без инвазивного вмешательства в тестовое
  окружение не входило в объём этого раунда. Требование «partial write
  failure produces non-matching observed checksum» доказано на уровне
  чистой функции — `scripts/lib/observedState.test.ts` — которая является
  ЕДИНСТВЕННЫМ местом, где observed checksum вычисляется (используется
  идентично и в apply, и в verify в `backfill-memberships.ts`), поэтому
  unit-доказательство переносится на реальный CLI без дополнительных
  предположений. Помечено как честный residual test gap, не скрыто.

## Исправления по итогам ПОВТОРНОГО независимого аудита

Второй раунд независимого ревью вернул `REVIEW_RESULT: CHANGES REQUIRED` с
ещё 7 категориями блокирующих замечаний — на этот раз глубже: недостаточная
строгость `verify`, duck-typing Timestamp, `unknownUsers`/`malformedClaims`
как чисто информационные (не блокирующие) записи, недостаточная повторная
проверка `confirm_role`, неполнота `decisionsChecksum`, делимитерный
`relationKey`, и `computeObservedState`, сравнивавший только role/status.
Все 7 исправлены в этом же PR, отдельным commit `fix(sec-005): close second
independent review findings` поверх HEAD
`8ad991a6affebe52233c0bfd88b5f25d9df24143`.

### 1. `verify` мог дать ложный PASS через совпадение ПУСТЫХ checksums

**Было**: `verification.matchesTarget` вычислялся ТОЛЬКО как равенство
`observedChecksum === targetChecksum`. Компания с нерешённым конфликтом и
без единого запланированного/пропущенного relation давала ПУСТОЙ
`targetChecksum` (хеш `[]`) — и если ничего не было применено,
`observedChecksum` тоже был хешем `[]`. Два пустых хеша совпадают →
`matchesTarget: true`, хотя план ВООБЩЕ не был разрешён.
**Стало**: `matchesTarget` теперь требует ОДНОВРЕМЕННО (`scripts/backfill-memberships.ts`):
`plan.applyAllowed` (план полностью разрешён — включая новый блокирующий
статус `unknownUsers`/`malformedClaims`, см. фикс #3 ниже), `missing.length
=== 0`, `differing.length === 0` и `observedChecksum === targetChecksum`.
`differing` сам стал строгим по схеме (фикс #7) — повреждённый документ
никогда не проходит, даже если role/status текстуально совпадают.

### 2. Timestamp проверялся duck-typing, а не реальным классом

**Было**: `isTimestampLike()` в `membershipValidation.ts` принимал ЛЮБОЙ
объект с числовыми `seconds`/`nanoseconds`, либо ЛЮБОЙ объект с функцией
`toDate` — то есть обычный JSON-объект `{seconds, nanoseconds}` или
поддельный `{toDate: () => new Date()}` проходили как валидный Timestamp.
**Стало**: `isRealTimestamp()` — `value instanceof Timestamp` (реальный
класс `firebase-admin/firestore`). Импорт класса не требует
инициализированного Admin SDK app — используется как чистое значение
(`Timestamp.now()`), поэтому модуль остаётся тестируемым без эмулятора.
Все "валидные" fixtures в unit-тестах (`membershipValidation.test.ts`,
`planner.test.ts`, `observedState.test.ts`) переведены на реальный
`Timestamp.now()`; добавлены отдельные тесты, доказывающие, что plain-map
и поддельный `toDate` теперь `invalid`.

### 3. `unknownUsers`/`malformedClaims` были чисто информационными

**Было**: `PlanResult.unknownUsers`/`malformedClaims` попадали в отчёт, но
НИКАК не влияли на `applyAllowed` — apply/verify могли пройти, даже если
пользователь без единой usable-relation или повреждённая `companies[]`-запись
были обнаружены.
**Стало**: `planner.ts`'s `applyAllowed` теперь требует
`unknownUsers.length === 0 && malformedClaims.length === 0`. Добавлен новый
безопасный механизм явного подтверждения — **user-level decision**:
`Decision.companyId` стал опциональным (`scripts/lib/types.ts`); decision
без `companyId` обязан иметь `resolution: 'exclude'`
(`scripts/lib/decisions.ts` валидирует это условие) и адресует запись по
`uid` напрямую (нет `companyId`, который можно было бы использовать для
адресации — у `unknownUsers`/`malformedClaims` его никогда не было).
Никакого молчаливого игнорирования не осталось: запись остаётся блокирующей,
пока либо не появится явный user-level `exclude` decision, либо не будет
исправлен источник данных (после чего запись просто перестанет
экстрактироваться).

### 4. `confirm_role` не перепроверял существование company/user

**Было**: (a) `planner.ts` применял `confirm_role` decision, доверяя
`companyId`/`uid` конфликта/anomaly без повторной проверки, что они
СЕЙЧАС существуют; (b) `legacyMapping.ts`'s id-mismatch-ветка создавала
`user_id_mismatch` conflict для ЛЮБОГО companyId в claims — включая
несуществующие — что означало, что `confirm_role` decision мог "создать"
membership под отсутствующей компанией.
**Стало**: (a) `planner.ts` добавил `confirmRoleTargetExists()` — перед
применением ЛЮБОГО `confirm_role` decision (и для conflicts, и для owner
anomalies) проверяется `allCompanyIds.has(companyId) && allUserIds.has(uid)`
(новый параметр `allUserIds` добавлен в `BuildPlanParams`, вычисляется в
`backfill-memberships.ts` из `readAllUsers()`); при несовпадении decision
игнорируется, запись остаётся unresolved. (b) `legacyMapping.ts` теперь
проверяет существование company ПЕРЕД классификацией id-mismatch claim: при
отсутствующей компании это `missing_company` **orphan** (никогда
`user_id_mismatch` conflict) — а orphans, по уже существующему правилу
`planner.ts`, разрешаются ТОЛЬКО через `exclude`, никогда через
`confirm_role`. Дополнительно: id-mismatch документ без единой usable claim
теперь тоже попадает в `unknownUsers`, а не исчезает молча.

### 5. `decisionsChecksum` был недостаточно нормализован

**Было**: `computeDecisionsChecksum()` принимала слабо типизированный
параметр (`{companyId, uid, resolution}[]`) и сортировала по составному
строковому ключу из ТОЛЬКО этих трёх полей — при этом на практике полный
объект Decision всё же попадал в хеш (JS не обрезает лишние поля), но
контракт функции этого не гарантировал и не документировал, и сортировка не
была защищена от структурных изменений вызывающего кода.
**Стало**: `computeDecisionsChecksum(decisions: readonly Decision[])` явно
строит канонический объект из ВСЕХ 7 полей (`uid`, `companyId ?? null`,
`resolution`, `role ?? null`, `reason`, `reviewedBy`, `reviewedAt`) и
сортирует по канонической JSON-строке САМОГО объекта (полностью
детерминированно и порядко-независимо, без опоры на уникальность частичного
композитного ключа). Обновлено неверное/неполное утверждение в
`docs/migrations/MEMBERSHIP_BACKFILL.md` (раздел «Counts and checksum
contract») — теперь явно перечисляет все 7 полей.

### 6. `relationKey` был delimiter-based (риск коллизии)

**Было**: `relationKey()` строил ключ через `companyId + "::" + uid`
(с `String.fromCharCode` вместо литерала — защита от порчи ИСХОДНОГО
кода, но НЕ защита от коллизии, если сам `companyId`/`uid` содержит `::`
— т.е. `relationKey('co', 'a::u1') === relationKey('co::a', 'u1')`).
**Стало**: `relationKey()`/`splitRelationKey()` (`scripts/lib/types.ts`)
используют канонический JSON tuple: `JSON.stringify([companyId, uid])`.
JSON-кодирование строк экранирует ЛЮБОЙ символ, который мог бы сделать
границу массива неоднозначной — коллизия невозможна КОНСТРУКТИВНО, а не
только "маловероятна". `splitRelationKey()` строго валидирует форму
результата (ровно 2-элементный массив строк) и бросает исключение на
любом другом вводе. Новый файл `scripts/lib/types.test.ts` — 9 тестов,
включая явные случаи с `::`, пробелами, JSON-значимыми символами и Unicode
(эмодзи, кириллица, японский) в обоих идентификаторах одновременно.

### 7. `computeObservedState` сравнивал только role/status

**Было**: документ считался `differing` ТОЛЬКО если его `role` или `status`
текстуально отличались от target — документ с ПРАВИЛЬНЫМИ role/status, но
чужим `uid`, поддельными/отсутствующими timestamps или лишними полями
проходил как совпадающий.
**Стало**: `observedState.ts` теперь вызывает
`isStrictlyValidActiveMembership(uid, data)` для КАЖДОГО прочитанного
документа — при провале строгой схемы документ помечается `differing`
НЕЗАВИСИМО от совпадения role/status. Схема также участвует в самом
checksum: `LogicalRelation` (`checksum.ts`) получил новое поле
`schemaValid?: boolean` (target — всегда `true` по построению; observed —
явно вычисляется), поэтому повреждённый документ даёт другой
`observedChecksum`, даже если по чистому role/status он "совпал бы".

### Новые red→green тесты

| Сценарий | Уровень | Файл |
|---|---|---|
| `verify` с повреждённой existing membership → ненулевой код | emulator | `backfill-memberships.emulator.test.ts` |
| `verify` с unresolved conflict и пустым target не проходит | emulator | `backfill-memberships.emulator.test.ts` |
| plain `{seconds,nanoseconds}` admin не проходит last-admin gate | emulator + unit | `backfill-memberships.emulator.test.ts`, `planner.test.ts`, `membershipValidation.test.ts` |
| поддельный `toDate()` не проходит как Timestamp | unit | `membershipValidation.test.ts` |
| unknown user блокирует И apply, И verify | emulator + unit | `backfill-memberships.emulator.test.ts`, `planner.test.ts` |
| user-level exclude decision снимает блокировку unknown user | emulator + unit | `backfill-memberships.emulator.test.ts`, `planner.test.ts`, `decisions.test.ts` |
| malformed claim блокирует apply | emulator + unit | `backfill-memberships.emulator.test.ts`, `planner.test.ts` |
| ID-mismatch пользователь без claims не исчезает → unknown | emulator + unit | `backfill-memberships.emulator.test.ts`, `legacyMapping.test.ts` |
| ID mismatch с missing company НЕ разрешается через confirm_role | emulator + unit | `backfill-memberships.emulator.test.ts`, `legacyMapping.test.ts`, `planner.test.ts` |
| смена `confirm_role.admin`→`confirm_role.viewer` меняет decisionsChecksum | emulator + unit | `backfill-memberships.emulator.test.ts`, `checksum.test.ts` |
| смена reason/reviewedBy/reviewedAt меняет decisionsChecksum | unit | `checksum.test.ts` |
| разные пары с `::`/пробелами/Unicode не конфликтуют | emulator + unit | `backfill-memberships.emulator.test.ts`, `types.test.ts` |
| observed membership с верными role/status, но неверным uid/timestamp/extra field не проходит verify | emulator + unit | `backfill-memberships.emulator.test.ts`, `observedState.test.ts` |

### Обновлённые счётчики тестов

| Suite | Файлы | Тестов | Результат |
|---|---|---|---|
| `scripts/lib/*.test.ts` (pure, no I/O) | 11 (+1 новый: `types.test.ts`) | 166 (было 127) | PASS |
| `scripts/backfill-memberships.emulator.test.ts` (real Firestore Emulator) | 1 | 39 (было 27, +12 новых) | PASS |
| **Итого `npm run test:migration`** | **12** | **205** (было 154) | **PASS** |

### Затронутые файлы этого раунда

Новый: `scripts/lib/types.test.ts`.
Изменённые: `scripts/lib/types.ts`, `scripts/lib/membershipValidation.ts`,
`scripts/lib/membershipValidation.test.ts`, `scripts/lib/legacyMapping.ts`,
`scripts/lib/legacyMapping.test.ts`, `scripts/lib/decisions.ts`,
`scripts/lib/decisions.test.ts`, `scripts/lib/checksum.ts`,
`scripts/lib/checksum.test.ts`, `scripts/lib/observedState.ts`,
`scripts/lib/observedState.test.ts`, `scripts/lib/planner.ts`,
`scripts/lib/planner.test.ts`, `scripts/backfill-memberships.ts`,
`scripts/backfill-memberships.emulator.test.ts`,
`docs/migrations/MEMBERSHIP_BACKFILL.md`, этот файл.
Без изменений вне scope: `src/**`, `functions/**`, `firestore.rules`,
`firestore.indexes.json`, `firebase.json` (подтверждено `git diff
origin/main...HEAD -- src functions firestore.rules firestore.indexes.json
firebase.json` — пусто).

### Повторный прогон всех проверок после исправлений

| Команда | Результат |
|---|---|
| `npm run typecheck` (root) | PASS |
| `npm run lint` (root) | PASS — 0 errors, тот же 1 pre-existing warning в `Balance.tsx` |
| `npx vitest run scripts/lib` | PASS — 166/166 |
| `npm run test:migration` (Firestore Emulator, JDK 21) | PASS — 205/205 (12 файлов) |
| `npm run test:unit` (root) | PASS — 147/147, без изменений |
| `npm run test:staging-preflight` | PASS — 5/5, без изменений |
| `npx tsc --noEmit -p tests/rules/tsconfig.json` | PASS |
| `npm run test:rules` (Firestore Emulator) | PASS — 77/77, без изменений |
| `npm run build` (root) | PASS |
| `npm ci` (root) | PASS |
| `npm audit` (root) | 22 уязвимости — без изменений от прошлого раунда (зависимости не менялись) |
| `npm ci --prefix functions` (portable Node 22.20.0) | PASS |
| `npm run lint --prefix functions` | PASS |
| `npm run typecheck --prefix functions` | PASS |
| `npm run build --prefix functions` | PASS |
| `npm run test:unit --prefix functions` | PASS — 68/68, без изменений |
| `npm run test:emulator --prefix functions` | PASS — 50/50, без изменений |
| `npm audit --prefix functions` | 11 (10 moderate, 1 high) — `functions/**` и его lockfile НЕ менялись этим раундом (подтверждено `git diff` — пусто); дельта от прошлого раунда (10 moderate) объясняется обновлением внешней advisory-базы npm между раундами, а не изменением зависимостей в этом PR |
| `git status --short` (до commit) | только файлы этого раунда, перечислены выше |
| `git diff --check` | только LF→CRLF предупреждения (Windows), не ошибки |
| `git diff origin/main...HEAD -- src functions firestore.rules firestore.indexes.json firebase.json` | пусто — подтверждено |

### Известные ограничения этого раунда

Ограничение из первого раунда (partial-write-failure на уровне live-emulator
race, доказано только на уровне чистой функции) остаётся в силе без
изменений — см. раздел выше «Известные ограничения этого раунда» (первый
раунд). Второй раунд не добавил новых ограничений: все 7 категорий
замечаний закрыты полностью, включая emulator-level доказательства, за
единственным исключением — прямое emulator-level доказательство
`computeObservedState`'s schema-strict `differing`-пути специфично для
сценария "документ был частью target на момент планирования, но повреждён
до read-back" (например, гонка между apply's create() и его же read-back).
Полный re-planning `verify`-прогон вместо этого ловит ЛЮБОЙ повреждённый
существующий документ ещё раньше — через
`classifyExistingMembership()` → `existing_membership_conflict` (сильнее,
чем `differing`: это блокирующий conflict, а не диагностика). Оба
emulator-теста для фикса #7 (`backfill-memberships.emulator.test.ts`)
явно документируют и используют этот более сильный, реально
срабатывающий путь; сам механизм `computeObservedState`'s schema-strict
`differing` при этом доказан исчерпывающе и напрямую на уровне чистой
функции (`observedState.test.ts`, 5 новых тестов).

## Исправления по итогам ТРЕТЬЕГО независимого аудита

Третий раунд независимого ревью вернул `REVIEW_RESULT: CHANGES REQUIRED` с
4 категориями блокирующих замечаний — на этот раз про целостность rollback
manifest, честность учёта после успешной записи, целостность уже
существующих (не мигрируемых заново) membership-документов и коллизию
сортировки. Все 4 исправлены в этом же PR, отдельным commit `fix(sec-005):
close third independent review findings` поверх HEAD
`ba210bbfbf814bd0e5ea9c6c44e3b889a15cd1a3`.

### 1. Rollback manifest мог быть неполным без обнаружения

**Было**: `validateSourceReportForRollback()` проверял только направление
«каждая запись `rollbackManifest` имеет соответствие в `createdPaths`» —
обратное направление не проверялось вообще. Отчёт с ДВУМЯ созданными
документами, но только ОДНОЙ записью в `rollbackManifest`, проходил
валидацию и производил rollback, который тихо удалял только ОДИН из двух
реально созданных документов, при этом сообщая об успехе.
**Стало**: `scripts/lib/rollbackValidation.ts` теперь: (a) строго
валидирует КАЖДУЮ запись `createdPaths` независимо (канонический путь, нет
дублей внутри массива) — раньше эти записи вообще не проверялись
самостоятельно, только использовались как источник lookup; (b) требует
ТОЧНОГО равенства множеств пар `(companyId, uid)` между `rollbackManifest`
и `createdPaths` — несовпадение размеров или состава в ЛЮБУЮ сторону
отклоняет весь отчёт; (c) пустой `rollbackManifest` признаётся валидным
ТОЛЬКО когда `createdPaths` тоже пуст И `report.counts.created === 0` —
усечённый/подделанный manifest с пустым списком, но с непустыми
`createdPaths`/`counts.created`, отклоняется явно; (d) каждая созданная
пара по-прежнему должна соответствовать РОВНО ОДНОЙ записи
`plannedCreates` — неоднозначная (дублирующаяся) запись `plannedCreates`
для одной пары теперь тоже отклоняется, а не молча берётся первая
попавшаяся. Любое несовпадение отклоняет ВЕСЬ отчёт до какого-либо
Firestore I/O — ноль удалений.

### 2. Успешный create() мог быть потерян из-за отдельного чтения

**Было**: `ref.create()` и последующий `ref.get()` (для получения
`createTimeIso`/`updateTimeIso`) находились в ОДНОМ `try/catch` в
`backfill-memberships.ts` — успешный create с последующим неудачным get
записывался как write failure и полностью пропадал из
`createdPaths`/`rollbackManifest`, хотя документ реально существовал в
Firestore с этого момента.
**Стало**: новый модуль `scripts/lib/applyWrites.ts` —
`createPlannedRelations()` определяет успех ИСКЛЮЧИТЕЛЬНО по собственному
`WriteResult` вызова `ref.create()` (поле `writeTime`, которое ДЛЯ ТОЛЬКО
ЧТО СОЗДАННОГО документа одновременно является и `createTime`, и
`updateTime`) — второго чтения для подтверждения успеха больше нет вообще,
поэтому "успешный create + неудачное последующее чтение" структурно не
может произойти на уровне этой функции. Отдельно, `readBackObservedState()`
оборачивает пост-записевый read-back (`readAllExistingMemberships()`) в
собственный try/catch — если ЭТОТ вызов целиком падает (а не отдельный
документ), это раньше приводило к необработанному rejection, которое
обрывало `main()` ДО вызова `writeReport()` — теряя весь отчёт, включая уже
известные `createdPaths`. Теперь такой сбой возвращается как явный
`{ok: false, error}`, отчёт всё равно записывается (с новым полем
`readBackError: string | null`, честно отражающим случившееся), и exit
code остаётся ненулевым. Дополнительно: `assertReportPathWritable()`
(`scripts/lib/report.ts`) пробным byte-less write+delete проверяет
записываемость пути отчёта ДО получения credentials/Firestore I/O; сам
`writeReport()` теперь пишет во временный файл и делает atomic rename —
crash посреди записи не может оставить усечённый/повреждённый отчёт.

### 3. Существующие "осиротевшие" membership не проверялись на целостность родителей

**Было**: `isStrictlyValidActiveMembership()` проверял ТОЛЬКО собственную
схему документа (uid совпадает, известная роль, `status: 'active'`,
настоящие Timestamp, нет лишних полей) — но НЕ то, что родительская
компания и указанный пользователь реально существуют. Из-за этого: (a)
membership-документ под несуществующей компанией мог "засчитаться" как
покрытие для `unknownUsers` (подавляя честную запись о нерешённом
пользователе); (b) валидно выглядящий admin-membership, ссылающийся на
несуществующего `users/{uid}`, мог удовлетворить last-admin-гейт для
РЕАЛЬНОЙ компании.
**Стало**: `firestoreReaders.ts`'s `computeExistingActiveAdmins()` получил
новый обязательный параметр `allUserIds` — admin membership теперь
засчитывается ТОЛЬКО если `uid` есть среди реально существующих
пользователей. `planner.ts`'s Step 6 (подавление `unknownUsers`) теперь
дополнительно проверяет `allCompanyIds.has(companyId)` перед тем, как
membership может "покрыть" uid. Новый Step 7 в `planner.ts` сканирует ВСЕ
`existingMemberships`: документ, который строго валиден сам по себе, но
ссылается на несуществующую компанию ИЛИ несуществующего пользователя,
теперь безусловно всплывает как блокирующий `OrphanRecord` (`reason:
'missing_company'` или `'missing_user'`) — той же моделью подтверждения
(`exclude` decision), что и любой другой orphan. Важно: `exclude`
decision снимает только САМ ПУНКТ СПИСКА orphan — независимо
вычисляемые факты («этот membership не считается admin», «этот membership
не подавляет unknownUsers») читают `allCompanyIds`/`allUserIds` напрямую и
никогда не зависят от того, был ли orphan подтверждён decision-ом.
`apply` и `verify` возвращают ненулевой код, пока проблема не исправлена
в данных или явно не подтверждена decision-ом.

### 4. Коллизия сортировки `plannedCreates`/`skipped`

**Было**: финальная сортировка использовала
`(a.companyId + a.uid).localeCompare(b.companyId + b.uid)` — пары
`companyId='a',uid='bc'` и `companyId='ab',uid='c'` обе конкатенируются в
`"abc"`, из-за чего компаратор не может их различить (относительный
порядок между ними становится недетерминированным/зависящим от порядка
входных данных).
**Стало**: `planner.ts` использует уже существующий
`sortRelations()` (`checksum.ts`) — тот же корректный, коллизионно-
безопасный двухпольный компаратор (сначала `companyId`, затем `uid`),
который уже применялся для checksums.

### Новые red→green тесты

| Сценарий | Уровень | Файл |
|---|---|---|
| Удаление одной из двух записей `rollbackManifest` отклоняется | emulator + unit | `backfill-memberships.emulator.test.ts`, `rollbackValidation.test.ts` |
| `createdPaths` с записью без соответствия в manifest отклоняется (обратное направление) | unit | `rollbackValidation.test.ts` |
| Пустой manifest с непустыми `createdPaths`/`counts.created` отклоняется | unit | `rollbackValidation.test.ts` |
| Дубли внутри `createdPaths` / неоднозначные `plannedCreates` отклоняются | unit | `rollbackValidation.test.ts` |
| `createPlannedRelations()` фиксирует успех по `WriteResult`, без второго `get()` | unit | `applyWrites.test.ts` |
| Успешный create не стирается последующим сбоем read-back | unit | `applyWrites.test.ts` |
| `assertReportPathWritable()` проверяет путь до I/O, не оставляет пробный файл | unit | `report.test.ts` |
| `writeReport()` — атомарная замена, без временных файлов | unit | `report.test.ts` |
| Existing membership под несуществующей компанией → блокирующий orphan, apply/verify ≠ 0 | emulator + unit | `backfill-memberships.emulator.test.ts`, `planner.test.ts` |
| Existing membership под несуществующей компанией НЕ подавляет unknownUsers | unit | `planner.test.ts` |
| Existing admin membership с несуществующим uid → блокирующий orphan, не проходит last-admin gate | emulator + unit | `backfill-memberships.emulator.test.ts`, `planner.test.ts`, `firestoreReaders.test.ts` |
| `('a','bc')` и `('ab','c')` не коллизируют, порядок детерминирован (оба направления входных данных) | emulator + unit | `backfill-memberships.emulator.test.ts`, `planner.test.ts`, `checksum.test.ts` |

### Обновлённые счётчики тестов

| Suite | Файлы | Тестов | Результат |
|---|---|---|---|
| `scripts/lib/*.test.ts` (pure, no I/O) | 15 (+4 новых: `applyWrites`, `firestoreReaders`, `report`, дополнения в существующих) | 202 (было 166) | PASS |
| `scripts/backfill-memberships.emulator.test.ts` (real Firestore Emulator) | 1 | 43 (было 39, +4 новых) | PASS |
| **Итого `npm run test:migration`** | **15** | **245** (было 205) | **PASS** |

### Затронутые файлы этого раунда

Новые: `scripts/lib/applyWrites.ts`, `scripts/lib/applyWrites.test.ts`,
`scripts/lib/firestoreReaders.test.ts`, `scripts/lib/report.test.ts`.
Изменённые: `scripts/lib/rollbackValidation.ts`,
`scripts/lib/rollbackValidation.test.ts`, `scripts/lib/report.ts`,
`scripts/lib/firestoreReaders.ts`, `scripts/lib/planner.ts`,
`scripts/lib/planner.test.ts`, `scripts/lib/checksum.test.ts`,
`scripts/backfill-memberships.ts`,
`scripts/backfill-memberships.emulator.test.ts`,
`docs/migrations/MEMBERSHIP_BACKFILL.md`, этот файл.
Без изменений вне scope: `src/**`, `functions/**`, `firestore.rules`,
`firestore.indexes.json`, `firebase.json` (подтверждено `git diff
origin/main...HEAD -- src functions firestore.rules firestore.indexes.json
firebase.json` — пусто).

### Повторный прогон всех проверок после исправлений

| Команда | Результат |
|---|---|
| `npm run typecheck` (root) | PASS |
| `npm run lint` (root) | PASS — 0 errors, тот же 1 pre-existing warning в `Balance.tsx` |
| `npx vitest run scripts/lib` | PASS — 202/202 |
| `npm run test:migration` (Firestore Emulator, JDK 21, прогнан дважды подряд для стабильности) | PASS — 245/245 (15 файлов) |
| `npm run test:unit` (root) | PASS — 147/147, без изменений |
| `npm run test:staging-preflight` | PASS — 5/5, без изменений |
| `npx tsc --noEmit -p tests/rules/tsconfig.json` | PASS |
| `npm run test:rules` (Firestore Emulator) | PASS — 77/77, без изменений |
| `npm run build` (root) | PASS |
| `npm ci` (root, неявно через предыдущие прогоны) | PASS |
| `npm audit` (root) | 22 уязвимости — без изменений (зависимости не менялись) |
| `npm ci --prefix functions` (portable Node 22.20.0, неявно) | PASS |
| `npm run lint --prefix functions` | PASS |
| `npm run typecheck --prefix functions` | PASS |
| `npm run build --prefix functions` | PASS |
| `npm run test:unit --prefix functions` | PASS — 68/68, без изменений |
| `npm run test:emulator --prefix functions` | PASS — 50/50, без изменений |
| `npm audit --prefix functions` | 11 (10 moderate, 1 high) — без изменений от прошлого раунда, `functions/**` не тронут (подтверждено `git diff` — пусто) |
| `git status --short` (до commit) | только файлы этого раунда, перечислены выше |
| `git diff --check` | только LF→CRLF предупреждения (Windows), не ошибки |
| `git diff origin/main...HEAD -- src functions firestore.rules firestore.indexes.json firebase.json` | пусто — подтверждено |

### Известные ограничения этого раунда

- Ограничения предыдущих раундов (partial-write-failure на уровне
  live-emulator race; `computeObservedState`'s schema-strict `differing`
  специфично для сценария "документ был частью target на момент
  планирования, но повреждён до read-back") остаются в силе без изменений
  — см. соответствующие разделы выше.
- **Rollback-manifest-completeness emulator-тест** доказывает удаление
  ОДНОЙ из ДВУХ записей вручную (`.filter()` над реальным отчётом от
  реального apply-прогона против эмулятора) — это прямое, честное
  доказательство обнаружения несоответствия «manifest ⊂ createdPaths», а
  не симуляция. Обратное направление («createdPaths содержит запись без
  соответствия в manifest») и остальные новые структурные проверки
  (дубли внутри массивов, неоднозначные `plannedCreates`, пустой
  manifest с непустыми `createdPaths`) доказаны исчерпывающе на уровне
  чистой функции (`rollbackValidation.test.ts`, 8 новых тестов) — тот же
  паттерн «pure-function unit proof + один end-to-end emulator-тест на
  главный сценарий», что и в предыдущих раундах.
- **"Create succeeded, subsequent read failed"** (фикс #2) доказан на
  уровне чистой функции через минимальный fake-Firestore-стаб
  (`applyWrites.test.ts`) — не через реальный emulator, так как
  детерминированно оборвать РОВНО одно чтение из реального emulator-запроса
  без инвазивного вмешательства в тестовое окружение не входило в объём
  этого раунда (тот же класс ограничения, что и partial-write-failure из
  первого раунда). Задание явно требовало "testable seams or pure helpers
  and tests" для этого пункта (не emulator-тест) — выполнено буквально.

## Коррекция: dangling memberships остаются fail-closed

Четвёртый раунд независимого ревью (по факту — повторное рассмотрение
того же PR после третьего раунда) обнаружил, что фикс #3 третьего раунда
(«existing orphaned membership integrity») сам содержал fail-open. Один
точечный commit `fix(sec-005): keep dangling memberships fail-closed`
поверх HEAD `fd95ac203da1be86adae1616f57c0cbce56450b1` устраняет проблему.

### Найденная проблема

**Было** (реализация третьего раунда, фикс #3): dangling existing
membership (документ, чья схема валидна сама по себе, но `companyId`/`uid`
не существует) добавлялся в `unresolvedOrphans` — тот же список, что и
legacy-source orphans, — и снимался ТЕМ ЖЕ `exclude` decision, что и
обычный orphan:
```ts
const decision = decisionByKey.get(key)
if (decision && decision.resolution === 'exclude') continue
```
Это позволяло pair-level `exclude` decision убрать dangling membership из
`unresolvedOrphans`, хотя документ физически оставался в Firestore. Если у
компании был ДРУГОЙ валидный admin (last-admin-гейт уже проходил
независимо от этого), единственным блокирующим сигналом для dangling
admin-membership оставался именно этот orphan-пункт — и после его
`exclude` `applyAllowed` мог стать `true`, а `verify` — вернуть success, с
повреждённым документом всё ещё на месте.

### Исправление

**Стало**: dangling existing memberships теперь репортятся в СОВЕРШЕННО
ОТДЕЛЬНОМ списке — `PlanResult.danglingMemberships` /
`report.danglingMemberships` (новый тип `DanglingMembershipRecord` /
`DanglingMembershipReason` = `'existing_membership_missing_company' |
'existing_membership_missing_user'`, `scripts/lib/types.ts`) — который
`decisionByKey`/`userLevelExcludedUids` НИКОГДА не консультируют ни при
каких условиях. Ни pair-level (`companyId`+`uid`), ни user-level (только
`uid`) `exclude` decision не может снять запись из этого списка. Единственный
способ снять блокировку — реально исправить данные (создать недостающую
компанию/пользователя или удалить сам dangling-документ) — тогда
СЛЕДУЮЩИЙ прогон просто не увидит эту запись, поскольку она вычисляется
заново каждый раз из фактического состояния Firestore.

`applyAllowed` теперь требует `danglingMemberships.length === 0`
безусловно. `counts.danglingMemberships` (новое поле `ReportCounts`)
честно отражает количество таких записей и учитывается в
`counts.unresolved`. `legacyMapping.ts`-orphans (`unresolvedOrphans`,
`OrphanRecord`/`OrphanReason`) остаются decision-resolvable ровно как
задокументировано — контракт для orphans, ссылающихся на ещё-не-мигрированные
legacy-claims (а не на физически существующие документы), не менялся.

### Новые red→green тесты

| Сценарий | Уровень | Файл |
|---|---|---|
| Компания с реальным валидным admin + dangling missing-user membership: pair-level exclude на dangling-пару не снимает блокировку | unit | `planner.test.ts` (требование #1) |
| Тот же сценарий: apply И verify возвращают ненулевой код, ноль записей, `matchesTarget === false` | emulator | `backfill-memberships.emulator.test.ts` (требование #2) |
| Membership под missing company + И pair-level, И user-level exclude одновременно — всё равно блокирует | unit | `planner.test.ts` (требование #3) |
| Валидная существующая membership (компания и user оба существуют) НЕ помечается dangling | unit | `planner.test.ts` |
| Уже невалидный (битая схема) существующий документ не дублируется этим шагом | unit | `planner.test.ts` |
| Существующие emulator/unit тесты третьего раунда обновлены на новое поле `danglingMemberships` (было `orphans`) | emulator + unit | `backfill-memberships.emulator.test.ts`, `planner.test.ts` |

### Обновлённые счётчики тестов

| Suite | Файлы | Тестов | Результат |
|---|---|---|---|
| `scripts/lib/*.test.ts` (pure, no I/O) | 14 (без новых файлов — только новые тесты в существующих) | 204 (было 202) | PASS |
| `scripts/backfill-memberships.emulator.test.ts` (real Firestore Emulator) | 1 | 44 (было 43, +1 новый) | PASS |
| **Итого `npm run test:migration`** | **15** | **248** (было 245) | **PASS** |

### Затронутые файлы

`scripts/lib/types.ts` (новый `DanglingMembershipRecord`/`DanglingMembershipReason`),
`scripts/lib/planner.ts` (Step 7 переписан), `scripts/lib/planner.test.ts`,
`scripts/lib/report.ts` (новые поля `danglingMemberships`
в `ReportCounts`/`MembershipBackfillReport`), `scripts/lib/report.test.ts`,
`scripts/backfill-memberships.ts` (счётчики, оба report-конструктора в
`runRollback`), `scripts/backfill-memberships.emulator.test.ts`,
`docs/migrations/MEMBERSHIP_BACKFILL.md`, этот файл.
Без изменений вне scope: `src/**`, `functions/**`, `firestore.rules`,
`firestore.indexes.json`, `firebase.json` (подтверждено `git diff
origin/main...HEAD -- src functions firestore.rules firestore.indexes.json
firebase.json` — пусто).

### Повторный прогон всех проверок

| Команда | Результат |
|---|---|
| `npm run typecheck` (root) | PASS |
| `npm run lint` (root) | PASS — 0 errors, тот же 1 pre-existing warning в `Balance.tsx` |
| `npx vitest run scripts/lib` | PASS — 204/204 |
| `npm run test:migration` (Firestore Emulator, JDK 21, прогнан дважды подряд) | PASS — 248/248 (15 файлов) |
| `npm run test:unit` (root) | PASS — 147/147, без изменений |
| `npm run test:staging-preflight` | PASS — 5/5, без изменений |
| `npx tsc --noEmit -p tests/rules/tsconfig.json` | PASS |
| `npm run test:rules` (Firestore Emulator) | PASS — 77/77, без изменений |
| `npm run build` (root) | PASS |
| `npm audit` (root) | 22 уязвимости — без изменений |
| `npm run lint --prefix functions` | PASS |
| `npm run typecheck --prefix functions` | PASS |
| `npm run build --prefix functions` | PASS |
| `npm run test:unit --prefix functions` | PASS — 68/68, без изменений |
| `npm run test:emulator --prefix functions` | PASS — 50/50, без изменений |
| `npm audit --prefix functions` | 11 (10 moderate, 1 high) — `functions/**` не тронут |
| `git status --short` (до commit) | только 7 изменённых файлов этого раунда |
| `git diff --check` | только LF→CRLF предупреждения (Windows), не ошибки |
| `git diff origin/main...HEAD -- src functions firestore.rules firestore.indexes.json firebase.json` | пусто — подтверждено |

### Известные ограничения

Без изменений от третьего раунда — см. соответствующий раздел выше. Этот
раунд был точечной коррекцией одного fail-open, обнаруженного в фиксе
третьего раунда, а не новой категорией находок.

## Staging authorization (EXTERNAL_ACTION_APPROVED: SEC-005)

Владелец репозитория передал явное разрешение:

```text
EXTERNAL_ACTION_APPROVED: SEC-005
ENVIRONMENT: staging
```

с явным запретом production в том же сообщении («Production запрещён»).
Реализовано и выполнено одним commit `fix(sec-005): allow staging
execution, keep production unconditionally blocked` поверх HEAD
`fc7d32fb9b3306e7f97063b82f93b6a33da0b948`.

### Что изменено в коде

`scripts/lib/firebaseAdmin.ts` — новая функция `assertCycleExecutionAllowed(environment)`,
намеренно отдельная от `assertEnvironmentGuard()` (та проверяет только
согласованность project ID, не авторизацию цикла на конкретное окружение).
`emulator` и `staging` теперь проходят этот gate; `production` **безусловно**
бросает `CycleExecutionError` — функция не читает
`--backup-reference`/`--rollback-reference`/`--ack-maintenance-readonly`
ни при каких условиях, ни один флаг не может изменить исход для production.

`scripts/backfill-memberships.ts`'s `main()` — заменён прежний
`if (environment !== 'emulator') return 4` на вызов
`assertCycleExecutionAllowed()` (тот же exit-код 4 при отказе, то же место
в потоке выполнения — после project-guard и production-флагов, до
`initFirestore()`/любого Firestore I/O).

### Почему production по-прежнему безопасен

`assertCycleExecutionAllowed('production')` бросает исключение
**безусловно**, независимо от того, переданы ли
`--backup-reference`/`--rollback-reference`/`--ack-maintenance-readonly`.
Существующая проверка этих флагов (строки выше в `main()`) осталась без
изменений и по-прежнему может первой вернуть код 3 при их отсутствии —
но даже при ВСЕХ трёх флагах, PRODUCTION_ACTION_APPROVED в этом цикле не
выдавался, поэтому `main()` в любом случае доходит до безусловного отказа
(код 4) раньше `initFirestore()`. Подтверждено emulator-тестом, реально
запускающим CLI-бинарник с этими тремя флагами против production — без
единого обращения к реальному Firestore (отказ происходит до
`initFirestore()`).

### Новые тесты

| Сценарий | Уровень | Файл |
|---|---|---|
| `assertCycleExecutionAllowed('emulator')` не бросает | unit | `firebaseAdmin.test.ts` |
| `assertCycleExecutionAllowed('staging')` не бросает | unit | `firebaseAdmin.test.ts` |
| `assertCycleExecutionAllowed('production')` бросает `CycleExecutionError` | unit | `firebaseAdmin.test.ts` |
| Production отклоняется (код 4) даже со ВСЕМИ approval-флагами — реальный CLI-бинарник, без единого обращения к Firestore | emulator | `backfill-memberships.emulator.test.ts` |
| Production отклоняется (код 4) для `--mode verify` без approval-флагов | emulator | `backfill-memberships.emulator.test.ts` |

**Почему staging-allow не доказан через реальный CLI-запуск в
автоматических тестах**: задание прямо ограничивает автоматический прогон
тестов эмулятором («Emulator only; no staging/production access»).
Реальный CLI-запуск с `--environment staging` неизбежно дошёл бы до
`initFirestore()`/попытки реального Firestore-чтения — недопустимо для
`npm run test:migration`, который обязан оставаться воспроизводимым,
быстрым и не зависящим от реальных GCP-credentials в CI. Поэтому
staging-allow доказан исчерпывающе и напрямую на уровне чистой функции
(`assertCycleExecutionAllowed('staging')` — та же функция, которой
реализован gate), а РЕАЛЬНОЕ end-to-end подтверждение staging получено
отдельно, в этом же цикле, через фактический authorized dry-run (см. ниже)
— именно тем механизмом, ради проверки которого функция и писалась.

### Обновлённые счётчики тестов

| Suite | Файлы | Тестов | Результат |
|---|---|---|---|
| `scripts/lib/*.test.ts` (pure, no I/O) | 14 (без новых файлов) | 208 (было 204) | PASS |
| `scripts/backfill-memberships.emulator.test.ts` (real Firestore Emulator) | 1 | 46 (было 44, +2 новых) | PASS |
| **Итого `npm run test:migration`** | **15** | **254** (было 248) | **PASS** |

### Затронутые файлы

`scripts/lib/firebaseAdmin.ts` (новые `assertCycleExecutionAllowed`/`CycleExecutionError`),
`scripts/lib/firebaseAdmin.test.ts`, `scripts/backfill-memberships.ts`
(gate заменён), `scripts/backfill-memberships.emulator.test.ts`,
`docs/migrations/MEMBERSHIP_BACKFILL.md`, этот файл.
Без изменений вне scope: `src/**`, `functions/**`, `firestore.rules`,
`firestore.indexes.json`, `firebase.json` (подтверждено `git diff
origin/main...HEAD -- src functions firestore.rules firestore.indexes.json
firebase.json` — пусто).

### Повторный прогон всех проверок

| Команда | Результат |
|---|---|
| `npm run typecheck` (root) | PASS |
| `npm run lint` (root) | PASS — 0 errors, тот же 1 pre-existing warning в `Balance.tsx` |
| `npx vitest run scripts/lib` | PASS — 208/208 |
| `npm run test:migration` (Firestore Emulator, JDK 21, прогнан дважды подряд) | PASS — 254/254 (15 файлов) |
| `npm run test:unit` (root) | PASS — 147/147, без изменений |
| `npm run test:staging-preflight` | PASS — 5/5, без изменений |
| `npx tsc --noEmit -p tests/rules/tsconfig.json` | PASS |
| `npm run test:rules` (Firestore Emulator) | PASS — 77/77, без изменений |
| `npm run build` (root) | PASS |
| `npm audit` (root) | 22 уязвимости — без изменений |
| `npm run lint --prefix functions` | PASS |
| `npm run typecheck --prefix functions` | PASS |
| `npm run build --prefix functions` | PASS |
| `npm run test:unit --prefix functions` | PASS — 68/68, без изменений |
| `npm run test:emulator --prefix functions` | PASS — 50/50, без изменений |
| `npm audit --prefix functions` | 11 (10 moderate, 1 high) — `functions/**` не тронут |
| `git status --short` (до commit) | только 4 изменённых файла этого раунда |
| `git diff --check` | только LF→CRLF предупреждения (Windows), не ошибки |
| `git diff origin/main...HEAD -- src functions firestore.rules firestore.indexes.json firebase.json` | пусто — подтверждено |

### Фактическое staging-действие: единственный dry-run против finapp-staging

Выполнен РОВНО один реальный вызов:

```bash
node scripts/backfill-memberships.ts \
  --environment staging --project finapp-staging --confirm-project finapp-staging \
  --mode dry-run --report-path <абсолютный путь вне репозитория, вне scratch-каталога сессии>
```

**Обезличенная сводка (единственное, что выведено в stdout —
`printSafeSummary()`; ни один uid/companyId в этом формате в принципе не
присутствует):**

| Поле | Значение |
|---|---|
| `mode` | `dry-run` |
| `environment` | `staging` |
| `projectId` | `finapp-staging` |
| `usersRead` | `0` |
| `companiesRead` | `0` |
| `existingMembershipsRead` | `0` |
| `candidateRelations` / `confirmedRelations` / `plannedCreates` | `0` / `0` / `0` |
| `created` / `skipped` / `conflicts` | `0` / `0` / `0` |
| `missingCompanies` / `missingUsers` / `ownerWithoutAdminMembership` | `0` / `0` / `0` |
| `unknownUsers` / `malformedClaims` / `danglingMemberships` | `0` / `0` / `0` |
| `unresolved` | `0` |
| `applyAllowed` | `true` (тривиально — источников для миграции нет) |
| exit code | `0` |

**Интерпретация**: `finapp-staging` в Firestore — существующий, реально
доступный (реальный ADC-аккаунт владельца репозитория, `gcloud auth list`
подтвердил активную авторизацию) проект, но на момент прогона в нём НЕТ ни
одного документа в `users`, `companies` или `companies/*/members` — то
есть либо ещё не заполнен, либо был очищен ранее. Инструмент отработал
штатно: 0 чтений → 0 кандидатов → пустой план → `applyAllowed: true` (нет
ни единого нерешённого пункта, потому что нет ни единого пункта вообще).

**Что НЕ выполнялось**: `--mode apply` против staging НЕ запускался — по
прямому указанию задания («Не выполняй apply, пока не покажешь
обезличенные счётчики dry-run»). Полный JSON-отчёт (`--report-path`)
записан ТОЛЬКО по абсолютному пути вне репозитория — не прочитан целиком
агентом, не выведен в чат/транскрипт, не закоммичен. В данном конкретном
прогоне полный отчёт и так не содержал бы uid/companyId (все счётчики
нулевые), но правило «никогда не открывать/не печатать полный отчёт»
соблюдено безусловно, а не только потому что в этот раз там нечему было
быть чувствительным.

### Известные ограничения этого раунда

- `finapp-staging` оказался пустым на момент dry-run — это НЕ проверка
  реальной миграции реальных legacy-данных (conflicts/orphans/decisions
  flow), а только подтверждение, что инструмент действительно
  устанавливает соединение, аутентифицируется и читает реальный внешний
  Firestore корректно и безопасно (0 записей, exit 0). Полноценная
  staging rehearsal с реальными legacy-данными (conflicts, decisions-file,
  apply) — если/когда `finapp-staging` будет заполнен данными или когда
  появятся данные для миграции — остаётся отдельным, не выполненным в этом
  цикле шагом, и по-прежнему требует отдельного явного `apply`-разрешения
  сверх уже выданного `EXTERNAL_ACTION_APPROVED: SEC-005 / ENVIRONMENT:
  staging` (которое в этом цикле трактовалось как разрешение только на
  dry-run, согласно прямому пункту 5 задания).
- Ограничения предыдущих раундов (partial-write-failure на уровне
  live-emulator race; dangling-memberships history) остаются в силе без
  изменений.

## Следующий разрешённый пункт
- Не `SEC-006`. Следующий шаг для SEC-005 — независимый review этого
  раунда (staging authorization + dry-run), затем (отдельным явным
  разрешением владельца, если потребуется) staging `apply` или production
  rehearsal. `SEC-006` не начат и не анализировался.
