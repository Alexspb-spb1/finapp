# SEC-006 — Secure one-time invitations (replacing password-based admin invites)

SEC-006 is implemented in staged PRs against one long-lived remediation task.
This report currently covers **Stage 2** (the `inviteMember` callable).
Stage 1 (data model, Zod schemas, deny-all Firestore Rules, unit+Rules
tests) was implemented, independently reviewed (`SEC_006_STAGE1_...PASS`),
and merged to `main` in [PR #19](https://github.com/Alexspb-spb1/finapp/pull/19)
(squash commit `0d3ee9498089d26cd90cdc5124d445b34e6c1082`) before this stage
began; no report file was produced for that stage at the time — its
deliverables are `functions/src/schemas/invitation.ts`,
`functions/test/unit/invitation-schema.test.ts`, and the two `invitations`/
`invitationLocks` deny-all blocks in `firestore.rules` plus their Rules
tests.

## Итоговый статус

READY_FOR_REVIEW (Stage 2 only — `inviteMember` callable)

## Branch / commit

- branch: `sec-006/stage-2-invite-member`
- base SHA: `0d3ee9498089d26cd90cdc5124d445b34e6c1082` (main, Stage 1 merge)
- result SHA: см. коммит, созданный сразу после этого отчёта в том же PR

## Проверенное исходное состояние

- Перепроверено: `main`@`0d3ee9498089d26cd90cdc5124d445b34e6c1082` — PR #19
  `MERGED`, checks `ci`/`functions`/`build`/`deploy` все `success` на этом
  коммите (fail-closed preflight перед созданием ветки).
- До этой задачи в пакете `functions/` не существовало ни одного callable,
  пишущего в `invitations`/`invitationLocks` — обе коллекции были deny-all
  и в Rules, и по факту (Stage 1). Baseline: полный набор тестов Stage 1
  (91 unit-тест схем, 126 Rules-тестов) зафиксирован как проходящий на
  этой базовой ревизии до начала Stage 2 (не изменён в этой задаче).
- Воспроизведение до реализации не требовалось (это новая функциональность,
    а не исправление дефекта) — вместо этого написаны emulator-тесты,
  проходящие через реальный Functions Emulator, доказывающие каждый
  требуемый инвариант (см. «Фактический вывод существенных тестов»).

## Что изменено

- `functions/src/lib/invitationToken.ts` (новый) — генератор raw-токена
  (32 случайных байта → base64url, 43 символа; источник энтропии
  инъецируемый — для детерминированных unit-тестов без вероятностной
  нестабильности), SHA-256 хеширование, и чистый билдер payload'а pending-
  приглашения (`buildPendingInvitationDocument`), который структурно не
  может получить raw token на вход — у него просто нет такого параметра.
- `functions/src/schemas/invitation.ts` — добавлена
  `InviteMemberResponseSchema` (`{ inviteId, token, expiresAtUtc }`,
  `.strict()`) рядом с уже существующей `InviteMemberRequestSchema`
  (Stage 1). Ни один из уже существующих Stage 1 экспортов не изменён.
- `functions/src/lib/errors.ts` — добавлен стабильный код
  `invitation_already_pending` → `HttpsError('already-exists')` (потребовал
  добавления `'already-exists'` в локально объявленный тип
  `HttpsErrorCode`). Ни одно существующее сопоставление код→HttpsError не
  изменено.
- `functions/src/index.ts` — новый экспортируемый callable `inviteMember`,
  реализующий полностью транзакционную модель создания приглашения +
  lock-документа + audit-события (см. «Security review» ниже за детали
  инвариантов).
- Тесты:
  - `functions/test/unit/invitationToken.test.ts` (новый) — генератор
    токена (точно 32 байта запрошено у инъецированного источника; формат;
    два вызова дают разные токены на реальном источнике), хеш (совпадает с
    прямым SHA-256; детерминирован), билдер payload'а (raw token
    структурно отсутствует; точная форма полей; успешный round-trip через
    `InvitationDocumentSchema`; принимает `FieldValue.serverTimestamp()`).
  - `functions/test/unit/invitation-schema.test.ts` — добавлены тесты
    `InviteMemberResponseSchema` (принимает точную форму; отклоняет
    tokenHash/email/companyId; отклоняет некорректный token; отклоняет
    отсутствующий expiresAtUtc). Существующие 91 тест не изменены.
  - `functions/test/unit/errors.test.ts` — добавлены тесты для
    `invitation_already_pending` (маппинг на `already-exists`; безопасное
    прохождение через `toSafeHttpsError`). Существующие тесты не изменены.
  - `functions/test/emulator/inviteMember.test.ts` (новый) — 22 теста
    через реальный Functions Emulator, покрывающие все 18 требуемых
    сценариев (детали в разделе «Фактический вывод существенных тестов»).
  - `functions/test/emulator/helpers.ts` — добавлены новые хелперы
    (`callInviteMember`, `getInvitationDoc`, `seedInvitationDoc`,
    `getInvitationLockDoc`, `seedInvitationLockDoc`, `countInvitationsFor`,
    `countAllInvitationLocks`, `authUserExistsWithEmail`,
    `signInAsExistingUser`). Ни один существующий экспорт не изменён.
- `functions/vitest.config.mts` — добавлен `fileParallelism: false`. Это
  понадобилось из-за находки во время прогона полного emulator suite (см.
  «Известные ограничения» / «Дополнительные находки») — без него два
  independent test-файла, оба трогающие один и тот же глобальный документ
  `system/maintenance` на одном реальном Firestore Emulator, изредка
  интерферировали друг с другом при параллельном запуске файлов.

## Почему изменения входят в текущий пункт

Все перечисленные файлы реализуют исключительно
`inviteMember({ companyId, email, role })` и его прямые зависимости
(генерация/хеширование токена, response-схема, один новый код ошибки,
тестовая инфраструктура для этого callable). `vitest.config.mts` — не
логика продукта, а исправление тестовой инфраструктуры, ставшее
необходимым строго из-за добавления второго emulator-тестового файла в
рамках этой же задачи (без него нельзя было бы честно подтвердить «полный
emulator suite дважды зелёный», что требуется чек-листом). Ничего из
`listInvitations`/`cancelInvite`/`resendInvite`/`previewInvite`/
`acceptInvite`, UI, отправки писем, создания Auth-пользователя, Rules для
`members`, деплоя — не реализовано и не тронуто (согласно явному
ограничению scope).

## Затронутые файлы

```text
functions/src/index.ts
functions/src/lib/errors.ts
functions/src/lib/invitationToken.ts        (new)
functions/schemas/invitation.ts             (see: functions/src/schemas/invitation.ts)
functions/test/emulator/helpers.ts
functions/test/emulator/inviteMember.test.ts (new)
functions/test/unit/errors.test.ts
functions/test/unit/invitation-schema.test.ts
functions/test/unit/invitationToken.test.ts (new)
functions/vitest.config.mts
```

## Критерии приемки

- [x] `inviteMember({ companyId, email, role })` — единственный новый
      callable; реализован, экспортирован из `functions/src/index.ts`.
- [x] Обязательная авторизация: `requireAuth` → `requireVerifiedEmail` →
      `validateRequest(InviteMemberRequestSchema, ...)`, затем внутри ОДНОЙ
      транзакции: `requireNotInMaintenanceMode` (первым), `requireActiveMember`,
      `requireRole(['admin'])`.
- [x] Токен: 32 случайных байта → base64url без padding (43 символа);
      персистится только SHA-256; raw token — только в локальной переменной
      обработчика и в единственном успешном ответе.
- [x] Токен/tokenHash/inviteId/expiresAt генерируются ОДИН раз до входа в
      транзакцию и переиспользуются при внутренних retry (структурно — по
      расположению кода — и эмпирически, тестом #14/#17).
- [x] Атомарность: приглашение + lock + audit — одна транзакция, либо всё,
      либо ничего (доказано тестами invalid-request/maintenance/corrupted-
      lock/insufficient-role — везде 0 записей при отказе).
- [x] Дублирование: `invitation_already_pending` для ещё не истёкшего
      pending-приглашения за тем же lock; атомарная замена lock для
      expired/accepted/revoked.
- [x] Response: строго `{ inviteId, token, expiresAtUtc }`, без tokenHash/
      email/companyId.
- [x] Ошибки: новый код `invitation_already_pending` → `already-exists`;
      повреждённое/несовпадающее состояние lock/invitation — общий
      `internal_error`, без утечки содержимого документа.
- [x] Полный список из 18 emulator-сценариев — реализован (см. ниже).
- [x] Полный список unit-требований к токену/хешу/response/ошибке —
      реализован.
- [x] Ни `listInvitations`/`cancelInvite`/`resendInvite`/`previewInvite`/
      `acceptInvite`, ни UI, ни отправка писем, ни создание Auth-
      пользователя, ни изменения Rules для `members`, ни какой-либо деплой —
      не реализованы (проверено: `git diff --stat` выше — только
      перечисленные файлы; никаких `firebase deploy`/`.rules` изменений).

## Проверки

| Команда | Результат | Примечание |
|---|---|---|
| `functions: npm run typecheck` | PASS | `tsc --noEmit`, чисто |
| `functions: npm run lint` | PASS | `eslint .`, 0 замечаний |
| `functions: npm run build` | PASS | `tsc`, чисто |
| `functions: npm run test:unit` | PASS | 183/183 (91 существующих + 92 новых/расширенных) |
| `functions: npm run test:emulator` (прогон 1) | PASS | 76/76 |
| `functions: npm run test:emulator` (прогон 2) | PASS | 76/76 |
| `root: npm run lint` | PASS | 0 ошибок, 1 pre-existing warning в `src/pages/Balance.tsx` (не тронут этой задачей) |
| `root: npm run typecheck` | PASS | `tsc -b`, чисто |
| `root: npm run test:unit` | PASS | 147/147 |
| `root: npm run build` | PASS | vite build успешен |
| `root: npm run test:rules` | PASS | 126/126, без изменений (Rules не тронуты) |
| `root: npm run test:migration` | PASS | 570/570 |
| `root: npm run test:staging-preflight` | PASS | 5/5 сценариев |
| `git diff --check` | PASS | пустой вывод, ошибок пробелов нет |
| `git status --short` | PASS | только ожидаемые файлы |
| secret/PII scan (grep по diff) | PASS | только синтетические `*@example.test`/`*@internal.example.test` адреса и уже существующие тестовые фикстуры; ни одного реального ключа/токена/PII |
| `test:e2e` | NOT AVAILABLE | в `package.json` такого script нет (то же самое было верно и для предыдущих SEC-00x задач) |

## Фактический вывод существенных тестов

```text
functions/test/unit: Test Files 9 passed (9); Tests 183 passed (183)
functions/test/emulator (прогон 1): Test Files 6 passed (6); Tests 76 passed (76)
functions/test/emulator (прогон 2): Test Files 6 passed (6); Tests 76 passed (76)
root src: Test Files 7 passed (7); Tests 147 passed (147)
root tests/rules: Test Files 1 passed (1); Tests 126 passed (126)
root scripts (migration): Test Files 24 passed (24); Tests 570 passed (570)
```

`functions/test/emulator/inviteMember.test.ts` покрывает построчно все 18
требуемых сценариев:

1. успешный вызов верифицированным admin создаёт ровно 1 invitation + 1 lock
   + 1 audit event
2. сохранённый хеш совпадает с возвращённым raw token
3. raw token отсутствует во всех Firestore-документах (сериализованная
   проверка invitation+lock)
4. неверифицированный admin — `email_unverified`, 0 записей
5. viewer/accountant — `insufficient_role`, 0 записей (`it.each`)
6. неаутентифицированный — `auth_required`, 0 записей
7. admin другой компании — `membership_not_found`, 0 записей
8. подделанные поля (`uid`/`status`/`createdAt`/`tokenHash`) — `invalid_request`, 0 записей
9. maintenance mode — `maintenance_mode`, 0 записей
10. повтор для того же pending — `invitation_already_pending`, исходные
    invitation/lock/audit не изменены
11. email с регистром/пробелами использует тот же lock
12. тот же email в разных компаниях — разные locks, оба вызова успешны
13. разные email в одной компании — оба успешны
14. настоящая конкурентность (2 запуска подряд) — ровно один успех, второй
    получает `invitation_already_pending`, без «сирот»
15. истёкшее pending-приглашение за lock — заменяется атомарно (и отдельно
    — уже принятое приглашение за lock — тоже заменяется атомарно)
16. повреждённый lock / отсутствующее целевое приглашение / несовпадение
    scope — везде generic `internal_error`, 0 новых записей, lock не
    изменён
17. форсированная конкуренция на read-set транзакции (параллельные записи
    в собственный membership-документ admin'а) — итоговое приглашение
    внутренне консистентно (хеш совпадает), без дублей
18. Auth-пользователь для приглашённого email не создаётся

## Security review

- Авторизация полностью fail-closed: любая ошибка чтения/валидации
  membership, lock или invitation документа приводит к отказу без записи
  (никогда не повышает права и не создаёт частичное состояние).
- `requireNotInMaintenanceMode` — первая операция внутри транзакции (через
  `txn.get()`), что закрывает TOCTOU-окно тем же механизмом, что и в
  `createCompany` (retry всей транзакции при изменении read-set).
- Raw token: генерируется один раз до транзакции; не имеет пути ни в один
  Firestore-документ (у `buildPendingInvitationDocument` физически нет
  параметра для него); не логируется; возвращается ровно один раз в ответе.
- Response — `.strict()`-схема, структурно не может содержать `tokenHash`.
- Повреждённые/несовпадающие lock/invitation документы никогда не
  раскрываются клиенту — единый generic `internal_error`, без содержимого
  документа, ID или указания, какой именно инвариант нарушен.
- `writeAuditEvent` вызван с той же транзакцией — audit event либо
  коммитится вместе с invitation+lock, либо не коммитится вовсе; в audit
  не попадают email/role/token/tokenHash.
- Ни один путь кода не создаёт Firebase Auth пользователя и не
  инициирует отправку письма (проверено тестом #18 и отсутствием таких
  вызовов в diff).

## Данные и миграция

Нет — это новая функциональность (создание документов в ранее deny-all
коллекциях), не миграция существующих данных. Idempotency здесь — не
обычная idempotency-key-схема (`runIdempotent`/`runBootstrapIdempotent`), а
доменная защита «один живой pending invite на (companyId, email)» через
`invitationLocks` — это осознанное архитектурное решение, зафиксированное
ещё в анализе SEC-006 (owner-approved v2/v3), а не отклонение от паттерна.

## Ручная проверка

Не проводилась — задача explicitly запрещает деплой/реальные Firebase-
операции; вся проверка — через Functions/Firestore/Auth Emulator (реальные
эмуляторы, не моки), что и требовалось.

## Rollback

Локально: `git checkout main` (ветка `sec-006/stage-2-invite-member` не
смержена). На уровне PR: закрыть PR без merge — в `main` при этом ничего не
меняется (Stage 1 уже смержен отдельно и не откатывается). После
гипотетического merge будущий rollback — revert одного squash-коммита;
`invitations`/`invitationLocks` остаются deny-all в Rules для клиентов
независимо от исхода (Stage 1 не меняется этой задачей).

## Известные ограничения

- `fileParallelism: false` в `functions/vitest.config.mts` — необходимое
  следствие появления второго emulator-теста, трогающего общий глобальный
  документ (`system/maintenance`); не является ослаблением существующего
  теста createCompany — сам этот тест и его ассерты не изменены, изменён
  только порядок выполнения файлов.
- Тест #17 (доказательство «retry не создаёт дублей») — честно
  задокументированное ограничение: у Firestore Emulator нет публичного
  хука для детерминированного форсирования внутреннего retry транзакции
  извне; тест форсирует контеншн на read-set транзакции реальными
  конкурентными записями и проверяет итоговую консистентность, что даёт
  сильную, но не 100% детерминированную эмпирическую проверку. Структурная
  гарантия (токен/inviteId генерируются один раз до `runTransaction` и
  захватываются замыканием — внутри тела транзакции для них просто нет
  кода генерации, который мог бы повторно выполниться) верифицируется
  чтением кода `functions/src/index.ts`, а не только тестом.
- Emulator-тесты (`test:emulator`) не запускались повторно после
  добавления отчёта — только после финальной версии кода/тестов/конфига,
  что и требовалось («после успешных проверок» коммит один).

## Дополнительные находки вне scope

- `createCompany.test.ts`'s TOCTOU-race тест (SEC-005/SEC-004, не тронут
  этой задачей) при параллельном запуске файлов с новым
  `inviteMember.test.ts` иногда не успевал поймать гонку (эмулятор под
  большей нагрузкой сдвигал тайминг), и при падении не успевал дождаться
  8 «хвостовых» вызовов `setMaintenanceMode(true)` до своего `afterEach`,
  что могло оставить `system/maintenance` в состоянии `enabled:true` для
  СЛЕДУЮЩЕГО теста в другом файле. `fileParallelism: false` устраняет
  наблюдаемый симптом (файлы больше не гонятся за один и тот же
  документ), но сам тест в `createCompany.test.ts` остаётся по
  конструкции временно́й гонкой (8 «выстрелов» без детерминированного
  хука) — не исправлялся в этом PR (чужой TASK_ID, не входит в SEC-006
  Stage 2).

## Diff summary

```text
 functions/src/index.ts                        | 113 +++++++++++++++++++++++++-
 functions/src/lib/errors.ts                   |   3 +
 functions/src/lib/invitationToken.ts          |  66 ++++++++++++++++ (new)
 functions/src/schemas/invitation.ts           |  15 ++++
 functions/test/emulator/helpers.ts            |  69 ++++++++++++++++
 functions/test/emulator/inviteMember.test.ts  | 390 ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ (new)
 functions/test/unit/errors.test.ts            |   8 ++
 functions/test/unit/invitation-schema.test.ts |  24 ++++++
 functions/test/unit/invitationToken.test.ts   | 120 +++++++++++++++++++++++++ (new)
 functions/vitest.config.mts                   |  16 ++++
 10 files changed
```

## Следующий разрешенный пункт

Не начинать. Следующие подпункты SEC-006 (в порядке из анализа/плана):
`listInvitations`, `cancelInvite`/`resendInvite`, `previewInvite`,
`acceptInvite`, фронтенд/UI, bootstrap-модуль для token-in-fragment.
