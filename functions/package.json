{
  "name": "finapp-functions",
  "private": true,
  "version": "0.0.0",
  "description": "SEC-003 — Cloud Functions authorization foundation for FinApp. Server-side authz helpers (requireAuth/requireVerifiedEmail/requireActiveMember/requireRole/assertNotLastAdmin/validateRequest/writeAuditEvent) plus a minimal read-only authorization probe callable. No privileged mutation Cloud Functions are implemented yet — that is SEC-004+.",
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit --pretty false",
    "lint": "eslint .",
    "test:unit": "vitest run test/unit",
    "test:emulator": "firebase emulators:exec --project demo-finapp --only auth,firestore,functions \"vitest run test/emulator\""
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.12.3",
    "eslint": "^10.3.0",
    "firebase": "^12.14.0",
    "firebase-tools": "15.24.0",
    "globals": "^17.6.0",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.59.2",
    "vitest": "^4.1.10"
  }
}
